---
description: Старт сессии — синхра репо (#032) + mailbox-check от brain + чтение SESSION_HANDOFF + re-триаж PENDING (#033)
---

Выполни старт сессии «Сабантуй в Малмыже» строго по шагам (детали — в `AGENTS.md` §📬 Mailbox check). Порядок жёсткий: **сначала синхронизация своего репо и почта (шаги 1–4), потом чтение session-памяти (шаги 5–6)** — pool #032.

1. **Sync свой репо — ПЕРВЫМ и ЕДИНСТВЕННЫМ:** `git fetch`; если working tree чист и есть отставание — `git checkout main && git pull --ff-only`. Незакоммиченное / не-ff — сообщи и не форсируй. Только после этого можно доверять `SESSION_HANDOFF`/`PENDING`.
   ❌ **Соседние репозитории (включая `../brain_matrica/`) не синхронизировать вообще** — ни `fetch`, ни `pull`, ни `checkout` (mandate владельца 2026-08-04: там может одновременно работать другой агент с незапушенной почтой).
2. **Скан входящих — из ДВУХ read-only каналов**, набор писем = объединение:
   - **локально:** корень `../brain_matrica/mailboxes/SabantuyMalmyzh/from-brain/*.md` — как есть, без синхронизации (НЕ `DRAFTS/`, НЕ `ARCHIVE/`);
   - **на GitHub `main`:** тот же путь в `brain_matrica` через API/веб, **без clone/fetch/pull** (`gh api repos/<owner>/brain_matrica/contents/mailboxes/SabantuyMalmyzh/from-brain?ref=main`).
3. **Свежесть — по каждому письму отдельно** (не по репозиторию): есть в одном источнике → берём его; совпадает → вопроса нет; **различается** → незакоммиченная локальная версия свежее, иначе сравнить последний коммит именно этого пути локально и на GitHub; неоднозначно → **прочитать обе версии, явно доложить конфликт, ничего не перезаписывать**.
4. **Доложи** пользователю сводку писем ДО чтения handoff, в формате:
   ```
   📬 N писем от brain_matrica:
   - [urgency COMPLIANCE] YYYY-MM-DD-slug — тема
   ```
   `urgency: high` — выдели отдельно даже если письмо одно. Для писем без `compliance`: `kind: directive`→MUST, `kind: idea`→SHOULD.
5. **Прочитай** `docs/SESSION_HANDOFF.md` — статус, текущая нитка, следующий шаг. Если `Updated:` старше 14 дней — пометь «handoff может быть неактуален», не доверяй слепо.
6. **Re-триаж отложенного (#033):** прочитай `docs/PENDING_FOLLOWUPS.md`. Всплывать **только пункты в состоянии `active`** с возрастом `added` > 30 дней ИЛИ `snoozed` ≥ 3× — **возраст считай из даты, слово-статус игнорируй**. Пункты `parked: <условие>` и `recurring: <ритм>` по возрасту НЕ всплывают: у `parked` проверь, не наступило ли **условие расконсервации** (наступило → всплыви), у `recurring` — не выбился ли **ритм прогонов** (`last-run`). Исходов четыре: возобновить / переформулировать под текущий код / **запарковать с проверяемым условием** / выкинуть (с причиной). Остальные пункты не перечисляй. Каждое осознанное «потом» — инкремент `snoozed` + обновление `last-touch`.
7. **Сводка main:** `git log --oneline -5` и `git status` — что нового, есть ли незакоммиченное.
8. Кратко предложи пользователю следующий шаг из handoff.

Не начинай правки кода до завершения шагов 1–5.
