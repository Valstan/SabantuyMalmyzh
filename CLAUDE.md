# CLAUDE.md — адаптер Claude Code

**Правила проекта живут в [`AGENTS.md`](AGENTS.md) — прочитай его и следуй ему.** Это единственный канон (ADR-0011); здесь только то, что специфично для Claude Code. Ничего из канона сюда не копировать: копия расходится с оригиналом молча (класс #087).

- **Исполняемые памятки:** [`.claude/commands/`](.claude/commands/) — `/start`, `/close_session`. Коммитятся, применимы любому агенту (правило перевода — в `AGENTS.md` §Совместная работа).
- **Ярусы автономии:** [`.claude/settings.json`](.claude/settings.json) — машиночитаемая форма ярусов из `AGENTS.md` §PR-only flow. Файл коммитится; персональные разрешения — в `.claude/settings.local.json` (gitignored).
- **В сомнениях** — задай вопрос пользователю и дождись явного ответа. На проде предположений не делаем.
