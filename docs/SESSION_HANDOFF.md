# SESSION_HANDOFF — записка между сессиями

> Читается на `/start` после синхры своего репо и mailbox-check. Обновляется в конце сессии.
> **Это sticky-note, а не летопись.** Только: где мы сейчас · что в работе · следующий шаг · свежие грабли.
> История — `git log -- docs/SESSION_HANDOFF.md` (там же весь прежний хронологический отчёт до 2026-08-10).
> Постоянные дома для остального: решения → [`docs/adr/`](adr/), отложенное → [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md), правила → [`../AGENTS.md`](../AGENTS.md), эксплуатация → [`docs/ops/`](ops/), переписка с мозгом → [`../mailbox/`](../mailbox/).
> Если `Updated:` старше 14 дней — не доверять слепо.

**Updated:** 2026-09-21 · **Branch:** main

---

## Где мы сейчас

Сайт **живой и в проде**: https://сабантуй.вмалмыже.рф (systemd + nginx на прод-боксе, деплой через `deploy-prod.yml` со смоуком). Сезон-2026 закрыт 04.07 — фестиваль прошёл, пост-фестивальный пакет сдан 11.07. Сейчас **межсезонье**: контент отдан, живут UGC-лента, фотостена (307 кадров), VK-вход посетителей, подписчики, новости, игры.

Прод несёт **живые пользовательские данные и ПДн** → воркфлоу, мутирующие прод-данные/env, и накат миграций — ярус `ask` (см. `AGENTS.md` §PR-only flow).

## Текущая нитка

**Сессия 2026-09-21 — security-мандат, D-093 приёмник конвейера, партия почты 05–18.09 целиком. PR #322–#331.**

- **Payload 3.90.1 / Next 15.5.24 на проде (#322, #326).** audit 0 critical / 0 high. Две миграции: `reset_password_requested_at` (G388) и `media._objectkey` — вторую детектор не увидел, потому что плагин storage-s3 локально выключен без S3-ключей; первый деплой упал на смоуке `/fotostena` (честный красный), исправлено. Security-заголовки стоят, `deps-audit.yml` пн/чт.
- **D-093 — приёмник `POST /api/ingest/posts` на проде (#328).** Контракт портала/Казанской; 401 без ключа (проверено с бокса); `INGEST_GATEWAY_KEY` на боксе и в комнате vault; **grant → setka предложен (201, alias `SABANTUY_INGEST_KEY`)** — ждёт принятия Сарафаном. Рубрика одна — `news`. Первый vitest в проекте (`lib/ingest.test.ts`), шаг `test` в гейтах.
- **D-088 / G312 (#329).** Корневой `canonical: '/'` наследовался девятью разделами — «25 дублей description» в Вебмастере. `lib/localeAlternates.ts`, у каждого раздела свой canonical/hreflang/description.
- **Канон (#329):** три правила D-096 (#125/#166/#152), память экосистемы и `brain_find.py` (D-097), список гейтов.
- **`probe-app.yml` (#323–#325, #330, #331)** — read-only probe: юнит, журнал `-o cat`, заголовки, `/fotostena`, `/api/media`, **#251 default_server, #250 заголовки ассетов**. Факты: TLS терминирует edge, nginx бокса слушает только :80; **по чужому Host сайт отдаётся** (nginx у ТАКСИ).
- **Письма brain: три** (security-отчёт; D-093; девять писем одним ответом с досрочными строками D-096/D-097). `ack` не осталось.

## Следующий шаг

1. **Ждём ответы:** Сарафан принимает grant `SABANTUY_INGEST_KEY` и гонит сухой прогон → первая живая доставка появится черновиком в `/admin` → Новости. ТАКСИ ставит `default_server` → повторить `probe-app.yml`, warning #251 должен исчезнуть.
2. **PENDING с датами (#152):** #015 таблица write-authz и `__Host-`-cookie + ssh `quiet` — **16.10**; #057 состязательный аудит — **30.11**.
3. **Уборка бокса сделана** (`cleanup-box.yml`, диск 68 % → 49 %); воркфлоу остаётся для будущих уборок: `plan` читает, `apply` удаляет только явный список.
4. **Не наш ход, но помнить:** D-072 (OIDC-клиент ЕСА) придёт одним письмом с реквизитами; переезд на ГОНБА отменён 2026-08-10 — не предлагать.

## Аналитика — закрыта полностью

Информер Метрики **на проде и показывает цифры** (просмотры/визиты/посетители за сегодня). LiveInternet снят, подпись автора стоит. Мандаты D-025, D-017 и «подпись автора» закрыты.

Что стоит помнить о самом информере: включает его **не** «доступ к открытой статистике» на вкладке Доступ, а чекбокс **«Информер»** в Настройки → Дополнительные настройки → «Код счётчика»; разметку брать из сгенерированного кабинетом кода; после включения Метрика пересчитывает информер около часа и всё это время отдаёт **200 и валидную картинку со сплошными нулями**. Проверять только глазами по картинке — код ответа ничего не доказывает.

Мелочь на будущее: информер стоит последним элементом подвала, и до нажатия «Понятно» его перекрывает плашка cookie. После закрытия плашки виден нормально.

## Открыто и чем разблокируется

| Что | Чем разблокируется |
|---|---|
| OIDC-клиент ЕСА (п.1 мандата 26.07) | Сарафан выдаёт `client_id`/`client_secret` (данные для регистрации отправлены 28.07) |
| Запись в каталоге сервисов ЕСА | правка у setka (наша ссылка на `/services` ведёт на новый домен) |
| **Хост комнаты vault в нашем публичном репо** (D-038, остаток) | brain/КАРМАН выбирают из трёх вариантов (письмо 31.08). Хостнейм **чужого** проекта в трёх местах; не вычищен сознательно — константа в `secretsBootstrap.ts` load-bearing, а чистка двух мест из трёх это театр |
| **Привязка ключа VK-шлюза к сообществу** | нужна ДО первого вызова с целью-владельцем, иначе 403 (шлюз fail-closed с 26.08). Кандидат Сарафана подтвердить или поправить |

Остальное отложенное — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) с состояниями и условиями расконсервации (механика #033 v2).

## Грабли, которые дороже всего забыть

- **Миграция схемы = три файла `.ts` + `.sql` + `.json`** (G192). Снапшота нет → drizzle диффит против пустоты → `down()` с `DROP TABLE … CASCADE` по всем таблицам. Отдельно: **initial-миграции у нас нет**, цепочка схему с нуля не воспроизводит; правда о схеме = конфиг + прод, сверка — `probe-schema.yml`. Детали: [`web/src/migrations/README.md`](../web/src/migrations/README.md).
- **PR-CI теперь ЕСТЬ** (`gates.yml`: с 2026-08-10 typecheck · lint · приёмка клиента vault, с 2026-08-31 **сканер секретов первым шагом**) — прежняя памятка «`gh pr checks` всегда пустой, гейт только локальный» устарела. Job называется `gates`, он **required-контекст `main` с `enforce_admins`** — то есть красный реально не даёт смержить. `build` с Postgres по-прежнему после мержа, в `deploy-prod`. `lint` гоняется с `--max-warnings 0`: без этого `next lint` отдаёт exit 0 при предупреждениях.
- **Сканер валит job по ИСТОРИИ ветки, а не по рабочему дереву.** Поймал строку → правка следующим коммитом **не помогает**, красный остаётся: `fetch-depth: 0` и `gitleaks git` смотрят все коммиты. Лечится пересборкой ветки (`git reset --soft origin/main` → один чистый коммит → новая ветка, т.к. force-push запрещён). Поймано на себе 31.08: гейт уронил письмо-отчёт о самом гейте, потому что оно цитировало строку дословно.
- **Ложный красный сканера лечить формой, а не каталогом.** Дефолтное `generic-api-key` срабатывает на поля с именем `key` и слагом-значением в сидах. Соблазн «исключить путь сидов» — ловушка: именно в SQL-**сиде** у соседа лежал живой ВК-токен. Исключения в `.gitleaks.toml` — по точной форме строки + `targetRules`, никогда по каталогу.
- **Логи прогонов ПУБЛИЧНЫ, и раннер печатает весь текст `run:`-блока.** Значит фильтрация вывода не закрывает канал: литерал в скрипте попадёт в лог независимо. Маскируется только `secrets` (`vars` печатаются как есть). Любой фильтр принимать **прогоном по настоящему выводу** с пустым грепом — по модели проверять бесполезно, я на этом ошибся дважды подряд.
- **Прод с этой машины виден только браузерным инструментом** — node fetch/curl не резолвят наши .рф-хосты; «недоступен» из curl — ложный сигнал. Кириллица в curl → mojibake, для API-тестов брать node fetch.
- **Видимое имя шапки/футера/героя рендерится из БД-глобалов**, не из кода — правка кода их не меняет, нужен точечный апдейт БД (`apply-brand.yml`, ярус `ask`).
- **`payload run` виснет молча** при расхождении конфиг↔dev-БД (интерактивный DROP-промпт) → сиды гонять с `NODE_ENV=production`; `build` при живом dev-сервере падает на `/_not-found` → `rm -rf .next`.
- **Детектор схемы видит только включённые плагины.** `generate:types` без S3-ключей не показал `_objectKey` от storage-s3 3.90, прод упал на `/api/media`. Перед бампом Payload гонять `S3_BUCKET=x S3_ACCESS_KEY_ID=x S3_SECRET_ACCESS_KEY=x pnpm generate:types` и читать диф как список нужных колонок.
- **Git Bash на Windows подменяет POSIX-пути в аргументах на путь под `C:/Program Files/Git/…`** (MSYS-конвертация путей) — `gh workflow run -f targets=<путь на боксе>` уехал на бокс с виндовым префиксом. Перед такими командами `export MSYS_NO_PATHCONV=1`. Спас гейт «все проверки до первого rm».
- **Проверка через nginx бокса — по http с Host-заголовком.** На :443 бокса никто не слушает (TLS на edge, G8), `https://127.0.0.1` даёт HTTP 000, а `-H Host` без SNI — пустоту. Два прогона probe отдали «ок» на пустоте; лечится контролем на своём домене (#284).
- **`gh workflow run` требует воркфлоу на `main`**: с ветки dispatch не работает (404). Read-only probe пришлось вливать PR'ом, зато он остался.
- **MEDIA_DIR** обязателен любому воркфлоу, который пишет в Media, иначе файлы лягут в эфемерный чекаут, а приложение отдаёт из персистентного `shared/media` → битые картинки при целых записях.
