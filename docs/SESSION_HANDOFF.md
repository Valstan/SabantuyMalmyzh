# SESSION_HANDOFF — sticky-note между сессиями

> Читается на `/start` **после** синхры с remote (порядок — pool #032) и после mailbox-check. Обновляется в конце сессии.
> История: `git log -- docs/SESSION_HANDOFF.md`. Если `Updated:` старше 14 дней — не доверять слепо.

---

**Status:** ✅ **PWA-установка + управление своим UGC-контентом — ЖИВЫЕ на ПРОДЕ** (2026-06-27, заявка владельца #2/#3/#4). **(#2, PR #166)** Кнопка «Установить приложение» в подвале: Android/Chrome — нативный промпт `beforeinstallprompt`, iPhone — модалка с инструкцией; PWA-обвязка уже была (manifest/sw/иконки), добавлена точка входа. **(#3, PR #167 backend + #168 UI)** «Удалить/править своё» в ленте: браузер генерит секрет `ownerToken` (localStorage) → шлёт заголовком `X-UGC-Owner` при создании → сервер стампит необратимый `ownerHash` (как ipHash, не PII); эндпоинты `/api/ugc/*` (`delete-submission` + удаление файла из S3, `delete-comment`, `edit-comment`, `unlike`) с authz **«свой токен ИЛИ персонал»** → автор удаляет/правит своё, **персонал в режиме «Редактирование» — любое** (= «админы удаляют чужое»); миграция `20260627_210000` (`owner_hash` + btree на submissions/comments/reactions; накат `--ref` ДО мержа по #017, deploy dispatch). UI: сердечко-тумблер лайк/отмена (оптимистик+откат), бейдж «Ваше» + 🗑 только на своём, inline-правка/удаление коммента. Backend-смоук dev **20/20**; UI-смоук preview (like/unlike тумблер; коммент create→edit→delete; удаление своей публикации — бейдж «Ваше»+корзина ТОЛЬКО на своих; серверная истина по каждому действию); прод-смоук зелёный (эндпоинты 400/404 как надо + `/lenta` 200). **Гибрид-владение** (решение владельца): аноним правит «своё» в браузере, VK-вход закрепит за аккаунтом. **(#4, VK ID вход — ОЖИДАЕТ ВЛАДЕЛЬЦА, owner-gated)** гайд [`docs/ops/vk-id-setup.md`](ops/vk-id-setup.md) написан; владелец регистрирует VK-приложение (тип «Веб», базовый домен `xn--80aaac1aqpgcf4bqn1j.xn--p1ai`, redirect `https://xn--80aaac1aqpgcf4bqn1j.xn--p1ai/api/auth/vk/callback`), передаёт **App ID → чат** + **Защищённый ключ → GitHub Secret `VK_CLIENT_SECRET`**; после этого PR #5 (коллекция `visitors` + своя сессия-cookie на `PAYLOAD_SECRET`, не мешая стафф-входу `/admin` + OAuth-callback с PKCE/state + закрепление контента за аккаунтом + имя/аватар). _Ранее (UGC-лента):_ ✅🎉 **«Народная лента» (UGC фото/видео) ЖИВАЯ на ПРОДЕ — PR1–PR6 + ключи S3 + CORS, e2e верифицирована** (2026-06-27). Ключи Object Storage владельца применены (через секреты GitHub + воркфлоу `apply-s3-keys`, не через чат); CORS поправлен под боевой домен; полная цепочка проверена: sign-upload→200, PUT в S3→200, public-read→200, CORS-preflight с apex→200. **Готова к использованию вживую** (бакет/лента чисты). Осталось желательное: **первый реальный пост владельца с телефона** + опц. модератор в день праздника. Владелец заказал интерактив: посетители снимают на телефон, выкладывают фото/короткие видео прямо на сайте (подготовка к Сабантую + сам праздник), лайки, комменты, шеринг — типа TikTok/VK-ленты. Решения владельца: **полная лента сразу**, **постмодерация + «пожаловаться»**, **прямая загрузка**, хранилище **Yandex Object Storage (S3)**. План: [`.claude/plans/sabantuy-ugc-feed.md`](../.claude/plans/sabantuy-ugc-feed.md). **PR1 (#147, на проде):** S3-обёртка `lib/s3.ts` + endpoint `POST /api/ugc/sign-upload` (presigned PUT — медиа грузится браузером НАПРЯМУЮ в бакет, минуя наш слабый бокс; без ключей → 503 degraded, проверено на проде). **PR2 (#149, на проде):** коллекция `submissions` (единица ленты) + миграция `20260627_090000` + хуки безопасности; прод-смоук `GET /api/submissions`→200 пусто, негативы consent/objectKey→400, sign-upload→503. **PR3 (#151, на проде):** коллекции взаимодействия `submission-reactions` (лайки+дедуп), `submission-comments` (постмодерация), `content-reports` (жалобы→авто-скрытие на пороге 3) + миграция `20260627_140000`; прод-смоук reactions/reports read→403, comments→200, негативы→400, несущ. цель→404. **PR4 (#153, на проде):** лента `/lenta`+`/tt/lenta` (force-static+revalidate 30, сетка видимых, сортировка Новое/Народный-выбор, фильтр фаз, медиа с S3, видео click-to-play) + ссылка в навигации + sitemap; прод-смоук 200, пустое состояние, **`x-nextjs-cache: HIT` + `s-maxage=30`** (отдаётся из ISR-кэша, бокс не грузится). **PR5 (#155, на проде):** UI загрузки (камера→presigned PUT в S3 с прогрессом→`POST /api/submissions`, даунскейл фото canvas, согласие 152-ФЗ) + кнопки лайк (оптимистик+дедуп localStorage)/коммент (тред)/жалоба/Web Share на карточках; e2e preview зелёный; прод-смоук `/lenta`→200 + upload-кнопка в SSR + ISR-HIT, sign-upload→503. **PR6 (#157, на проде):** полировка — тизер «Народной ленты» на главной (CTA `/lenta`, по образцу тизера игры) для discoverability к празднику; письмо brain (переносимый паттерн presigned-direct-S3-UGC + закрытие их идеи #4/I8). **S3-активация (2026-06-27, эта сессия):** владелец создал бакет `sabantuy-malm` + сервис-аккаунт + статический ключ; ключи доставлены в прод-env через **зашифрованные секреты GitHub** (`S3_ACCESS_KEY_ID`/`S3_SECRET_ACCESS_KEY`) + разовый воркфлоу [`apply-s3-keys.yml`](../.github/workflows/apply-s3-keys.yml) (#158, SSH→дописал 6 S3-строк в `/etc/sabantuy/sabantuy.env`+рестарт; секрет не в чат/репо, #008). **CORS-фикс:** владелец задал `*.домен` (поддомен-wildcard), но браузер для IDN шлёт **punycode apex** Origin → preflight 403; поправил CORS бакета через S3-API (`PutBucketCors`, ключи вытянул с бокса в процесс, на экран не печатал) на `https://xn--80aaac1aqpgcf4bqn1j.xn--p1ai` + поддомены → preflight 200. **e2e на проде зелёный** (sign→PUT→public-read→CORS). Бакет/лента очищены от тест-объекта. **Желательное (не блокер):** первый реальный пост владельца с телефона; опц. модератор онлайн в день праздника (постмодерация «виден сразу»). _Ранее в эту сессию (на проде): пред-фестивальный probe (вердикт ВЫДЕРЖИТ) + 2 perf-митигации — cache-headers (#144) и ISR force-static (#145); см. нитку ниже._ _(Ранее: анонс «Сабантуй-2026»+афиша — PR #137–141; игры 33/10 — #132/#134; on-site редактирование — #127–130.)_

> **Заметка по next/script (кандидат в GOTCHAS):** inline-`<Script>` в **серверном** компоненте (наш `Analytics`/Метрика) попадает в статический HTML; в **клиентском** (`'use client'`, наш `LiveInternetCounter` в подвале SiteChrome) — next/script инжектит скрипт client-side, в SSR-HTML его НЕТ (видна только разметка-бейдж). Поэтому прод-смоук «grep по HTML» на LI-скрипт даёт ложный «absent» — проверять надо в живом DOM (preview): src бейджа свопается на `counter.yadro.ru/hit?t29.6`. Метрика (серверный Analytics) в HTML видна, LI (клиентский подвал) — нет, хотя оба работают. **Доп. (PR #118):** «бейдж LI не виден» оказался **рунет-анти-баннером** (режет `counter.yadro.ru`), не кодом; счётчик работает, недосчёт — норма для LI (источник правды трафика — Метрика).
**Updated:** 2026-06-27
**Branch:** main

## Текущая нитка

**Сессия 2026-06-27i — PWA-установка (#2) + управление своим UGC (#3) + гайд VK ID (#4).** Заявка владельца: (#2) установить сайт как приложение на телефон; (#3) удалять свои файлы/комменты, править комменты, отменять лайки, админы — чужое; (#4) быстрый вход через VK. Решения владельца (AskUserQuestion): владение — **«аноним + VK закрепляет»** (гибрид); VK — **«напишу инструкцию, вы регистрируете»**. **PR #166 (#2):** `InstallAppButton` в подвале (SiteChrome) — Android/Chrome ловит `beforeinstallprompt`→нативный промпт, iPhone→модалка-инструкция, standalone/неподдержка→не рендерит; PWA-обвязка уже была. Preview: синтет. `beforeinstallprompt`→кнопка появляется, `prompt()` зовётся, скрывается; чистая консоль. **PR #167 (#3 backend):** поле `ownerHash` (text+index, field-access adminOrEditor) на submissions/comments/reactions + миграция `20260627_210000`; `hashOwner`/`ownerHashFromHeaders` в `lib/ugc`, `deleteObject` в `lib/s3`, `lib/ugcOwner` (getPayloadClient/isStaffRequest/mutateRateOk); 4 эндпоинта `/api/ugc/*` (authz «свой токен ИЛИ персонал», idempotent, rate-limit). Хуки `stampSubmissionMeta`/`stampIpHash` стампят ownerHash из `X-UGC-Owner`. Dev-смоук **20/20**. Накат миграции `apply-migration --ref feat/ugc-ownership` (человеч. гейт #017 — спросил через AskUserQuestion) → merge → `deploy-prod` dispatch (обошёл migration-guard). **PR #168 (#3 UI):** `ugcClient` (getOwnerToken/ugcHeaders/markMine/isMine + клиенты мутаций); LentaCard — тумблер лайк/отмена, 🗑 на своём/персоналу-в-manage, бейдж «Ваше», onRemoved; LentaComments — inline edit/delete своего/любого-персоналу; i18n+CSS. Preview-смоук + серверная истина зелёные (примечание: dev Fast-Refresh re-mount от ISR-снимка иногда «возвращал» удалённую карточку — артефакт только dev, в проде HMR нет). **(#4)** гайд `docs/ops/vk-id-setup.md` — ждёт регистрации VK-приложения владельцем (App ID→чат, секрет→GitHub Secret `VK_CLIENT_SECRET`). **Авто-мерж под зелёными гейтами** (mandate 2026-06-25) — в репо нет PR-CI, гейт = локальные typecheck+lint+build + deploy-prod build/smoke на мерже.

**Сессия 2026-06-27h — прямой эфир `/efir` через VK Live (#162, на ПРОДЕ).** Владелец выбрал live через VK. Object Storage эфир не вещает → встраиваем внешний VK-плеер; наш бокс не нагружается. **`globals/LiveStream`** (`live_stream`): `isLive`/`vkUrl`/`note`, `read=anyone`/`update=adminOrEditor`, `afterChange→revalidateLiveStream` (/efir+/tt/efir мгновенно); миграция `20260627_200000` (одна таблица, push-inspect, verify diff 1:1 кроме pg_dump17-nonce). **`lib/vkEmbed`** — парсит `vk.com`/`vkvideo.ru` (`video{oid}_{id}`/`video_ext.php`) → безопасный iframe-src из oid/id/hash; **анти-инъекция:** произвольный/не-VK src не подставляем (→ заглушка), даже из /admin. **`_views/EfirView`+`/efir`+`/tt/efir`** (ISR rev 30): адаптивный 16:9 VK-плеер при `isLive`+валидной ссылке, иначе заглушка «трансляция не идёт». Нав-ссылка «Эфир», sitemap, i18n `efir.*`. **Проверка:** гейты зелёные (`/efir` ○ Static rev 30s); preview — заглушка, плеер с корректным VK-src (`oid=-12345&id=67890` из `vk.com/video-12345_67890`), не-VK→заглушка, обе локали, ноль ошибок; прод `/efir`+`/tt/efir`→200, ISR-HIT, глобал в `/api/globals/live-stream`. **Эксплуатация:** организатор в `/admin → Прямой эфир` вставляет ссылку на VK-трансляцию + ставит «Эфир идёт» → плеер на `/efir` (выключил → заглушка). **Доп. (#164):** VK-CTA на `/efir` — кнопка «Прямые эфиры в ВКонтакте» → сообщество (`lib/site VK_COMMUNITY_URL`, на телефоне приложение VK). **⚠️ Разобрана хотелка «браузерный лайв от посетителей в 1 клик + авто-сохранение»:** турнкей не выходит — Cloudflare WebRTC-запись в бете (coming soon) + западные сервисы (Cloudflare/Mux/api.video) не оплатить из РФ; вынесено в **пост-фестивальный проект** (свой стриминг-сервер MediaMTX/аналог на росс. VPS) — см. PENDING. _UGC-нить + live (в реалистичном к празднику объёме) закрыты._

**Сессия 2026-06-27g — встроенная камера ленты (#160, на ПРОДЕ).** По заявке владельца («снимать прямо с сайта, не только выбор файла»). **`components/LentaCamera.tsx`:** `getUserMedia`-видоискатель в модалке — фото (кадр из потока через canvas, jpeg q0.9), видео (`MediaRecorder`, таймер + авто-стоп 60с), смена камеры (front/back), аудио только в видео-режиме; готовый `File` → общий поток `measureAndPrepare`→presigned PUT в S3; cleanup треков; нет камеры/доступа → graceful-fallback «недоступна → Выбрать файл». `lib/ugcClient`: `pickRecorderMime` (mp4 первым ради iOS, webm-фолбэк) + `baseMime` (срезает `;codecs=…`, иначе `kindOf`/`isAllowedMime` не матчат запись MediaRecorder). `LentaUpload`: выбор источника «Снять на камеру»/«Выбрать файл», `onPick`→общий `handleFile`. i18n `lenta.cam.*`, CSS `.lenta-cam-*`. **Проверка:** гейты зелёные (`/lenta` 120kB); preview — выбор источника, камера монтируется и в headless (нет камеры) уходит в fallback, возврат к форме, ноль ошибок; прод `/lenta`→200, video sign-upload→200. Живой видоискатель/запись — на реальном телефоне. **⚠️ «Стрим» (живая трансляция зрителям) — Object Storage НЕ умеет** (это ingest+транскодинг+CDN, отдельная тяжёлая инфра); реальный путь для live — embed VK Live/Rutube (организатор вещает с телефона, мы показываем плеер). Не делали — ждёт решения владельца, нужен ли live-эфир (vs. съёмка-клипов, которая уже есть). _Кросс-платформа видео: предпочитаем mp4/h264 (играет везде); если устройство пишет только webm/vp8 — iOS-зрители не проиграют (транскод на боксе не тянем; приемлемо для MVP)._

**Сессия 2026-06-27f — S3-активация «Народной ленты»: ключи + CORS, фича ЖИВАЯ (e2e на проде).** Владелец прошёл консоль Yandex Cloud (с моей пошаговой навигацией — прямые ссылки/скриншоты): создал бакет **`sabantuy-malm`** (public-read на объектах) + CORS + сервис-аккаунт `sabantuy-ugc-writer` (роль `storage.editor`) + статический ключ. **Доставка ключей в прод (секрет НЕ в чат, #008):** владелец положил `S3_ACCESS_KEY_ID`/`S3_SECRET_ACCESS_KEY` в **зашифрованные секреты GitHub** (Settings→Secrets→Actions, браузер), я сделал разовый воркфлоу [`apply-s3-keys.yml`](../.github/workflows/apply-s3-keys.yml) (#158): SSH (изолированный ключ #001) → `sed` снёс старые S3-строки + дописал 6 (4 литерала + 2 из `${{ secrets }}`) в `/etc/sabantuy/sabantuy.env` → рестарт; verify sign-upload≠503. Прошёл: 6 S3-строк, sign-upload→**200** с presigned-URL. **⚠️ CORS-грабля (IDN/punycode, кандидат в GOTCHAS/brain):** владелец задал origins `*.домен` (поддомен-wildcard), но **браузер для IDN-домена шлёт punycode-apex** `https://xn--80aaac1aqpgcf4bqn1j.xn--p1ai` (без поддомена) → CORS-preflight `OPTIONS` с этого Origin → **403** (wildcard `*.` не матчит apex). Браузерная загрузка была бы заблокирована. Лечение: `PutBucketCors` через S3-API (на боксе `@aws-sdk` вшит в бандл, не отдельным модулем → запускал со своей машины, ключи вытянул с бокса в env процесса по SSH, **на экран не печатал**) → origins `https://xn--80aaac1aqpgcf4bqn1j.xn--p1ai` + `https://*.…` → preflight **200** (`Access-Control-Allow-Origin: <apex>`). **e2e на проде зелёный:** sign-upload→200, **PUT файла в S3→200**, public-read→200 (image/jpeg), CORS-preflight apex→200. Тест-объект удалён (бакет 0 объектов), лента пуста. **Фича готова к живому использованию.** _Дальше — желательный первый реальный пост владельца с телефона; иначе UGC-нить закрыта._

**Сессия 2026-06-27e — «Народная лента» PR5/6: UI загрузки + взаимодействие (#155, на ПРОДЕ).** Завершает интерактив. **Загрузка** (`lib/ugcClient.ts`+`components/LentaUpload.tsx`): камера/галерея (`<input accept=image/*,video/*>`), клиент-валидация типа/размера + видео ≤60с (`<video>.duration`) + даунскейл фото canvas до 1600px (экономия egress); **presigned PUT напрямую в S3** с прогресс-баром (XHR) → `POST /api/submissions`; согласие 152-ФЗ; модалка (reuse `edit/Modal`); коды ошибок→i18n; без ключей→sign-upload 503→degraded. **Взаимодействие** (`components/LentaCard.tsx`+`LentaComments.tsx`): лайк (оптимистик+дедуп `localStorage ugc-liked:<id>`+POST reaction, 409 оставляет/иные откат), комменты (раскрываемый тред, ленивый fetch видимых + форма), жалоба (POST+`localStorage ugc-reported:<id>`), Web Share. `LentaFeed` стал клиентским контейнером (items state + prepend на загрузке; upload-кнопка и при пустой ленте); `LentaView` всегда рендерит контейнер; тип `LentaItem`→`lentaTypes.ts` (без циклов). i18n `lenta.upload.*`/действия (tt черновик), CSS `.lenta-*`. **Проверка:** гейты typecheck+lint+build зелёные (`/lenta`,`/tt/lenta`=○ Static rev 30s, +8kB First Load JS); **e2e preview** (snapshot/click, 5 dev-записей через временный `S3_PUBLIC_BASE_URL`, потом убран) — лайк (оптимистик 20→21 + POST + recount likeCount=1 + дедуп), коммент (toggle→fetch→add, текст в БД, recount commentCount=1), жалоба (POST→report_count=1→UI «✓»), модалка загрузки (file input `accept=image/*,video/*`/согласие/submit-гейт/подсказка), обе локали, **ноль ошибок консоли**. Реальный PUT в S3 — после ключей владельца. **Прод (без миграции→авто-деплой):** `/lenta`→200, upload-кнопка «Поделиться кадром» в SSR, ISR-HIT+s-maxage=30, sign-upload→503, `/tt/lenta`→200. _Следующий — PR6 (полировка + письмо brain)._ **⚠️ Сид-артефакт:** dev-сид ставил фейковые `like_count`/`comment_count` без backing-строк → первый реальный лайк/коммент пересчитывает счётчик по факту (recount = COUNT); на проде счётчики стартуют с 0 и консистентны.

**Сессия 2026-06-27d — «Народная лента» PR4/6: публичная лента `/lenta`+`/tt/lenta` (#153, на ПРОДЕ).** Только отображение (кнопки лайк/коммент/жалоба + загрузка — PR5). **`_views/LentaView`** (сервер): `force-static`+`revalidate=30`, тянет видимые `submissions` (`withRetry`, как `GalleryView`), маппит в `LentaItem` с медиа-URL из Object Storage (`publicUrl`) — медиа НЕ через бокс, браузер грузит напрямую с S3, бокс отдаёт лёгкий кэшированный HTML. **`components/LentaFeed`** (`'use client'`): сортировка Новое/Народный-выбор (по `likeCount`) + фильтр фаз — на клиенте по ISR-снимку (роут остаётся статически кэшируемым, без `searchParams`). Карточка: фото `<img>` / видео **click-to-play** (без автоплея, монтируется по клику — экономия egress), бейдж фазы, автор, подпись, счётчики (read-only); graceful-fallback на битое медиа. `/lenta`+`/tt/lenta` роуты, `lentaMeta` (canonical+hreflang), i18n `lenta.*` (tt черновик), ссылка «Лента/Тасма» в шапке/подвале (footer-nav сделан устойчивым к числу пунктов), `/lenta` в sitemap, CSS `.lenta-*`. **Проверка:** гейты typecheck+lint+build зелёные; `/lenta`+`/tt/lenta`=○ (Static) revalidate 30s; preview на dev (5 dev-записей, медиа на `/public` через временный `S3_PUBLIC_BASE_URL`) — сетка/чипы/бейджи/авторы/видео click-to-play/пустое состояние/обе локали, ноль ошибок (HTML-верификация — превью-скриншот в окружении вырожден). **Прод (без миграции → авто-деплой напрямую):** `/lenta`+`/tt/lenta`→200, пустое состояние, **`x-nextjs-cache: HIT`+`s-maxage=30`** (ISR-кэш, бокс не грузится), sitemap содержит `/lenta`. _Следующий — PR5 (UI загрузки + кнопки взаимодействия)._

**Сессия 2026-06-27c — «Народная лента» PR3/6: реакции/комменты/жалобы + миграция (#151, на ПРОДЕ).** Три коллекции взаимодействия поверх `submissions`, все **non-versioned** (G7). **`submission-reactions`** (лайки): `create:anyone`, read/upd/del `adminOrEditor`; дедуп «один на IP» (хеш IP в beforeValidate→409), `afterChange/afterDelete` пересчитывают `submissions.likeCount` **COUNT'ом** (без гонок инкремента); лайк только видимой публикации (`assertVisibleTarget`). **`submission-comments`** (постмодерация): `create:anyone`, `read:publicVisibleOrStaff`, upd/del `adminOrEditor`; rateLimit+sanitize+стоп-мат, `afterChange→commentCount` (COUNT видимых); служебные поля field-access (#015). **`content-reports`** (жалобы, полиморфно `targetType`+`targetId`): `create:anyone`, read/upd/del `adminOrEditor`, дедуп (цель+IP)→409; `afterChange` считает `reportCount` и на пороге `UGC_REPORT_HIDE_THRESHOLD`(3) **авто-скрывает** цель (`status=hidden`+`hiddenReason`). Общее: вынес `clampText` в `lib/ugc` (DRY с PR2) + `relId`+`UGC_REPORT_TARGETS`; `hooks/ugcHelpers` (`assertVisibleTarget`), `stampIpHash`. **Миграция `20260627_140000`** (.ts+.sql): 3 таблицы + 2 enum'а + FK на `submissions` (ON DELETE set null, как drizzle) + связки; push-inspect, **verify down→up→diff 1:1**, идемпотентно. **⚠️ Поймано смоуком (Payload-транзакционность):** `count/update/findByID` в хуках **обязаны получать `req`** — иначе идут вне текущей транзакции, не видят только что вставленную строку → **счётчики отставали на 1** (likeCount 1 вместо 2, авто-скрытие не срабатывало). Исправлено `req` во всех local-API вызовах. **Проверка:** гейты зелёные; **e2e-смоук** (UTF-8 тела файлами — урок G11; XFF для разных IP) — лайк+дедуп→409+likeCount=2, коммент чистый/мат→400/пусто→400+commentCount=1, **3 жалобы→auto-hide** (status=hidden, reportCount=3), скрытый→404, лайк скрытого→409, доступ #015. **Прод-роллаут (#017):** apply-migration `--ref` → merge #151 → `deploy-prod --ref main` (dispatch) зелёный; прод-смоук reactions/reports→403, comments→200, негативы→400, несущ. цель→404. _Следующий — PR4 (лента `/lenta`)._

**Сессия 2026-06-27b — «Народная лента» PR2/6: коллекция `submissions` + миграция + хуки (#149, на ПРОДЕ).** Коллекция `submissions` — единица ленты (фото/видео посетителей), **non-versioned** (обход G7); медиа НЕ в БД и НЕ через бокс — в записи только `objectKey` в Object Storage. Поля: `kind/objectKey(unique)/posterKey/mime/bytes/width/height/durationSec/authorName/caption/phase/consent` + служебные `status`(visible default — постмодерация)/`hiddenReason/likeCount/commentCount/reportCount/ipHash/userAgent`. **Доступ (#015):** `create:anyone`, `read:publicVisibleOrStaff` (аноним видит только `status=visible`, персонал — всё), `update/delete:adminOrEditor`; служебные поля закрыты field-access на запись анонимом (анти-подмена), `ipHash/userAgent/reportCount/hiddenReason` — и на публичное чтение. **Хуки:** `rateLimitSubmission` (beforeValidate: rate-limit/IP + валидация objectKey-формата=анти-чужой-путь + mime/размер + санитайз + токен-стоп-фильтр мата `lib/profanity.ts`), `stampSubmissionMeta` (beforeChange: необратимый хеш IP `hashIp` + user-agent). Новые `lib/ugc.ts` (фазы/статусы/лимиты/clientIp/hashIp), `access/publicVisibleOrStaff.ts`. **Миграция `20260627_090000`** (.ts+.sql): таблица `submissions` + 3 enum'а (kind/phase/status) + связка locked_documents_rels; push-inspect (G81/R10), **verify down→up→pg_dump diff 1:1**, идемпотентно. **Проверка:** гейты typecheck+lint+build зелёные; локальный API-смоук на живом dev — consent→400, чужой objectKey→400, мат→400, размер>лимит→413, **anti-spoof** (аноним не задаёт status/счётчики/ipHash — сохраняется visible/0/0, хеш ставит сервер), **read-фильтр** (hidden→404, аноним-список только visible). **Прод-роллаут (#017):** `apply-migration --ref feat/ugc-submissions-collection` (накат таблицы ДО мержа) → merge #149 → авто-деплой упал на migration-guard (ожидаемо, коммит вносит файл миграции) → `deploy-prod --ref main` (dispatch, guard пропускает) зелёный. Прод-смоук: `/api/submissions`→200 пусто, негативы consent/objectKey→400, sign-upload→503, таблица пуста. **⚠️ Грабля смоука (G11-семья):** инлайн-curl с кириллицей в Windows-консоли шлёт **cp1251**-байты → сервер декодит как UTF-8 → мождибейк в `caption` → профанити-фильтр видел мусор и пропускал мат (ложный «баг кода», увёл в отладку regex/SWC). Лечение: **тела с кириллицей писать в файл (UTF-8) и слать `curl --data-binary @file`** — тогда мат корректно→400. _Следующий — PR3._

**Сессия 2026-06-27 — «Народная лента» (UGC фото/видео), старт фичи; PR1/6 на ПРОДЕ.** Владелец заказал интерактив-обмен фото/видео (типа TikTok/VK). **Разведка (3 агента):** GONBA и MatricaRMZ хранят медиа на **Я.Диске** (REST+OAuth), но для публичной видео-ленты на слабом боксе правильнее **Object Storage (S3)** — стабильные URL, браузер тянет напрямую из Яндекса, наш Node не грузится. GONBA даёт переносимые UGC-кирпичики (`Messages` с модерацией `isModerated`, rate-limit); у нас уже есть свой `PollVotes`/`QuizResults` (агрегат+дедуп для лайков). Brain флагнул это как идею #4/I8 «VK-UGC фотостена» (after-season) — делаем раньше. **Решения владельца:** полная лента, **постмодерация + «пожаловаться»**, прямая загрузка, **S3**. **Архитектура (план `.claude/plans/sabantuy-ugc-feed.md`):** медиа НЕ через наш Node (presigned PUT браузер→S3 на загрузке; публичный URL с S3 на просмотре; лента ISR force-static). Коллекции (non-versioned, обход G7): `submissions`/`submission-reactions`/`submission-comments`/`content-reports` (паттерн `create:anyone`+rate-limit-хук+`ipHash`+field-access #015). Постмодерация-предохранители: согласие+аффирмация, жалоба→авто-скрытие на пороге, мгновенное скрытие в /admin, rate-limit, стоп-фильтр мата. **PR1 (#147, на проде):** deps `@aws-sdk/client-s3`+presigner; `lib/s3.ts` (path-style, `presignUpload`/`buildObjectKey`/`publicUrl`, graceful-degradation); `POST /api/ugc/sign-upload` (валидация kind/mime/размер+rate-limit→presigned PUT; без ключей→503); `.env.example` S3_*; `next.config` remotePatterns `storage.yandexcloud.net`. Прод-смоук: endpoint без ключей → **503** «Хранилище пока не настроено» (корректно). **🔑 Owner-критпуть:** настроить Object Storage (`docs/ops/yandex-object-storage-setup.md`) — без ключей живая загрузка не работает; имя бакета → мне, ключи → прод-env. **Следующий PR — PR2** (коллекция `submissions` + миграция push-inspect G81/R10 + хуки). Гейт PR1 (typecheck+lint+build) зелёный.

**Сессия 2026-06-26 — пред-фестивальный capacity-probe (директива brain `2026-06-26-prefestival-probe-before-peak`, recommend/high; 8 дней до 4 июля).** Снял с бокса по SSH (localhost, обход G8). **Числа:** baseline `available`=1254 МБ, Next RSS=242 МБ; под нагрузкой 40 конкурентных curl×75с — пик RSS **295 МБ** (+53), мин `available` **1041 МБ**, **0 рестартов** (PID стабилен → OOM не было), load до ~1.9 (1 vCPU насыщен), swap=0 подтверждён. На юните обнаружен **`MemoryMax=1 GiB` (cgroup)** — защита: процесс убьётся cgroup-OOM на ~1 ГБ и рестартнётся, не утянув бокс; `--max-old-space-size` поэтому избыточен. ISR работает (`x-nextjs-prerender:1`, `s-maxage=60, SWR`; warm-TTFB низкий/стабильный; RSS плоский под нагрузкой). **Бутылочное горло — vCPU/latency, не RAM:** throughput плато ~18 req/s, latency растёт линейно с очередью (conc1=64мс → conc40=1785мс mean, p95 2384мс). **Вердикт: ВЫДЕРЖИТ** (RAM с запасом; режим отказа в пик = медленные страницы, не падение; swapless-риск не срабатывает — heap плоский на ISR). Ack brain с числами: `mailbox/to-brain/2026-06-26-prefestival-capacity-probe.md`; follow-up с ISR-находкой: `mailbox/to-brain/2026-06-26-isr-force-static-dynamic-routes.md`. **По итогу probe владелец одобрил 2 митигации, обе на ПРОДЕ:** **(PR #144)** `headers()` в `next.config` — длинный `max-age=604800` для `/public`-статики (`/afisha /decor /quiz /icons`) вместо дефолтного `max-age=0` (афиша 405КБ шла через Node на каждый просмотр); НЕ immutable (same-name замены, напр. калфак); `/media`/`/sw.js`/`/og.jpg` не тронуты. **(PR #145, КРУПНЕЙШИЙ рычаг)** probe вскрыл: **контентные страницы `[slug]`/`[game]` отдавались `no-store` — ре-рендер на КАЖДЫЙ хит** (динам. сегмент без `generateStaticParams`; `generateStaticParams` бесполезен — CI собирает против пустой БД, deploy-prod.yml:98), тогда как статич. роуты (`/`,`/gallery`,`/igra`) кэшировались. Фикс — `export const dynamic = 'force-static'` (паттерн GONBA) на 8 роутах (`[slug]`/`igra[game]`/`gallery[slug]`/`events[slug]` + tt): `next build` флипнул ƒ→○; хуки `revalidatePageDoc`/`revalidateEvent` дополнены tt-зеркалами (мгновенные on-site правки в обеих локалях; `revalidateQuiz` уже покрывал tt). **Прод-смоук:** `/sabantuy-2026`,`/faq`,`/narody`,`/igra/sabantuy`,`/tt/*` → `no-store`→`s-maxage=60` (MISS→HIT), контент цел (Калинино/4 июля, 36КБ). **Выигрыш:** `/sabantuy-2026` warm TTFB **206мс→3.7мс**; под 30-conc **~18→81 rps, 1785→5мс mean**. Горячая анонс-страница теперь из ISR-кэша. **Заодно подтверждено:** SMTP-обвязка (`nodemailerAdapter` под гейтом `SMTP_HOST` в `payload.config.ts`) уже в коде — console-fallback degraded; остаётся только владелец (`SMTP_*` в прод-env). **Пиковый probe в день 4 июля остаётся в PENDING** (валидация модели под реальной нагрузкой). _Мелкий хвост: у Gallery нет revalidate-хука → on-site правка альбома применяется ≤60с (теперь gallery/[slug] ISR); добавить `revalidateGallery` если правки альбомов участятся (в PENDING-инж.)._

**Сессия 2026-06-25 — анонс «Сабантуй-2026» + официальная афиша (PR #137–141, всё на ПРОДЕ).** Заявка владельца: написать про надвигающийся Сабантуй в Малмыже 4 июля 2026, «чтобы завлекательно», со ссылкой на источник; дал ссылку на свою группу ВК и файл афиши. **Ход:** (1) сначала просил подать «приедет губернатор» — веб-проверка (RuNews24/Татар-информ): визит **в открытых источниках не подтверждён** (А. Соколов открывал Сабантуй в Вятских Полянах-2025, награждал малмыжан — реальный прецедент; Всероссийский-2026 уехал в Пензу). Через `AskUserQuestion` выбрали framing «традиция/ожидание» + размещение «страница Pages + блок на главной». **PR #137:** страница `slug=sabantuy-2026` (`seedAnnouncement.ts`, ru+tt-черновик, без миграций — только Pages; идемпотентна, `SEED_FORCE`), `pageDecor` запись (шапка = кадр `page-maydan`), i18n `home.anons.*`, тизер в `HomeView` (gated на `getAnnouncement` → нет окна 404 между deploy и сидом), workflow `seed-announcement.yml`. (2) Владелец прислал **официальную афишу + текст оргкомитета** → **PR #138:** переписал страницу под подтверждённые факты (дата/время/место/программа/гости), губернаторскую линию **убрал** (в афише не упомянут; гости — артисты Рамазановы), тизер/title обновил. (3) **PR #139:** FAQ «когда и где» → с. Калинино/10:00 (ru `seedCulture` + tt `seedTatar`); чтобы force-сид не клоббил прочие культ-страницы — добавлен фильтр **`SEED_ONLY=slug[,slug]`** + input `only` в `seed-culture.yml`/`seed-tatar.yml` (+ `seed-tatar.yml` в allow). (4) **PR #140:** дал URL группы ВК (`vk.com/malm4317sabantuikazanskaya`) → строка-источник стала **кликабельной ссылкой** (custom-link Lexical, форма как `lib/lexical-lite.ts`); попутно фикс: `inline()` в сидах парсит только `**bold**`, а `_..._` показывался литеральными подчёркиваниями. (5) Дал **файл афиши** → **PR #141:** оптимизировал (sharp → `web/public/afisha/afisha-sabantuy-2026.{webp,jpg}`, ≤1080px, коммитятся как декор), показ вертикального постера **целиком** через новое опц. поле **`pageDecor.poster`** + `<figure.page-poster>` в `PageView` + CSS. **Грабли:** (a) вставленная в чат картинка приходит как vision-инпут, **не файл на диске** — нужен путь/URL от владельца (или on-site upload); временные `.tmp` в `%TEMP%` оказались не цельными файлами. (b) Прод-роллаут: PR #137/#141 — код+ассет (деплой), #138/#140 — контент (`seed-announcement.yml force=1`), #139 — контент (`seed-culture only=faq force=1` + `seed-tatar only=faq`). **Проверка:** локально preview по каждому PR (страница/тизер/FAQ/ссылка/постер — рендер, ассеты 200, картинка реально грузится 1024×1535 webp, 0 ошибок консоли); гейты typecheck+lint+build зелёные. Прод: deploy-смоук + box-смоук `/sabantuy-2026`+`/tt`=200 (внешний хит с этой машины местами рубит G8 — сверял со стороны раннера). **tt — черновик** (вычитка в PENDING).

**Сессия 2026-06-24/25 — расширение и доводка игр (PR #132/#134, всё на ПРОДЕ).** Заявка: «доводка хвостов-долгов» → расширение банка вопросов (PENDING/handoff-кандидат #2), затем по фидбэку владельца — чистка картинок «Угадай по картинке». **(A) «Знаток Сабантуя» 16→33 (PR #132):** +17 выверенных вопросов через workflow (fan-out draft по темам + независимый **adversarial-фактчек** по источникам ru.wikipedia/wiktionary/tatarstan.ru/culture.ru/visitkirov.ru: верный ответ подтверждён, дистракторы проверены как неверные, у каждого `source`). Заведена тема `language` (су/икмәк/рәхмәт — кроссязычные translate: на /tt промпт татарский, варианты русские, поэтому `tt.options=ru.options`). **Механика:** колода за партию = случайная выборка `QUIZ_DECK_SIZE=12` (`lib/quiz.ts → effectiveDeckSize`, общий для `QuizView→getQuizStats` и `QuizGame→slice`, чтобы `total` статистики === длине партии); банк <12 берётся целиком (картиночная не ломается). Без миграции (поля + `game` default уже на проде) — чистый аддитивный сид. **(B) «Угадай по картинке» 8→10 + чистка фото (PR #134):** аудит всех 8 картинок визуально (Read jpg напрямую, G44/G82) — 7 годных, неудачный калфак (мелкий ч/б портрет, убор не читался) → крупный музейный кадр синего бархатного калфака (CC BY-SA 4.0); +тюбетейка (CC0) +гармонь-тальянка (PD). Картинки искал через workflow (6 параллельных ресёрчеров по Commons), лицензии/URL сверял по **Commons API** (`action=query&prop=imageinfo&iiprop=url|extmetadata`), каждый итоговый файл визуально проверил. Чужие-festival кадры (масленичный столб, шотландский канат) отбраковал — для игры про Сабантуй это новый mismatch. Без миграции; накат `seed-quiz-images.yml force=1` (меняется картинка калфака + 2 новых). **Грабля (письмо brain `2026-06-25-wikimedia-commons-recipes.md`):** Commons 429 на пачке скачиваний — IP-wide, держится минуты даже с backoff; `process-quiz-images.mjs` укреплён (пауза+ретрай), обход — реюз уже скачанных копий + Commons API для лицензий. **Проверка:** локально preview (текстовая: колода 12/33, проход→«X/12», статистика total=12; картиночная: 10 вопросов, kalfak/tubeteika/garmon грузятся 200, интегрити 10/10, 0 ошибок консоли); typecheck+lint+build зелёные. Прод: deploy (смоук зелёный) → сиды (создано 17 / 10, смоук игр зелёный).

**Сессия 2026-06-19b — on-site редактирование контента (PR #127/#128/#129/#130, всё на ПРОДЕ).** Заявка владельца: править сайт «на ходу», не заходя в `/admin` (вход на сайте + тумблер «Просмотр/Редактирование» + правка текста/заголовков/картинок на месте, охватить как можно больше). Эталон — **GONBA R2** (`brain_matrica/cross-project-ideas/REFERENCE.md`); план `.claude/plans/partitioned-brewing-gadget.md` (апрувнут). Сделано в 3 PR по плану. **Общая инфраструктура** (`web/src/lib/me.ts`, `lib/lexical-lite.ts`, `app/(frontend)/components/edit/*`): `AdminMode` (контекст isAdmin+режим, localStorage), `LoginControl` (модалка `POST /api/users/login`, cookie `payload-token`, остаёмся на странице), `EditToolbar` (верхняя панель + тумблер), `Modal` (a11y focus-trap, замена shadcn), `InlineImage` (`POST /api/media`), `LiteRichTextEditor` (contentEditable+тулбар+inline-картинки). Локаль из пути (`pathLocale`), PATCH `?locale=` + `_status:'published'` для versioned-коллекций. **PR #127 (фундамент)** — редактор страниц (Pages): заголовок+тело+обложка; **поле `Pages.heroImage`** + миграция `20260619_140000`. **PR #128** — редакторы Событий/Галерей (массив фото add/remove/replace/подпись/порядок, строки по id in-place)/Карты (глобал, POST), без миграций. **PR #129** — текст главной/шапки/подвала → **глобалы `home`/`header`/`footer`** (overlay по `key`: иконки/href/порядок в коде, текст в БД; чтение `home?.x || t(...)`, фолбэк на код → ISR-safe; layout тянет header/footer обе локали для единого SiteChrome; глобалы **без drafts**). Миграция `20260619_150000` (12 таблиц) + сид `seedSiteContent.ts` (`seed-site-content.yml`, идемпотентно по key). **Грабли (в коде, письмо brain #130 → GOTCHAS):** (1) media URL **по имени файла** (`doc.url`), `/api/media/file/{id}` → 500 — превью/inline берут реальный url, контент грузим `depth=1`; (2) Lexical upload-узел `value` **числом** (integer PK) + `version:3`, иначе «not a valid upload ID». **Проверка:** локально preview — вход/режимы, правка каждого типа (текст без потерь, inline-картинка=валидный upload-узел и рендерится, галерея сохраняет id строк, глобалы идемпотентны 6/8/6, **пустой глобал→сайт идентичен**); typecheck+lint+build зелёные, ISR цел. Прод-роллаут #017: миграции `--ref` ДО мержа → deploy-prod --ref main → seed-site-content; смоук раннеров `/`+`/tt`+`/map`+`/admin`=200, глобалы засижены. **SSH-смоук с этой машины — G8 (edge myjino режет SSH/HTTPS), проверка шла со стороны GitHub-раннеров.** **Письмо brain #130** (`2026-06-19-onsite-editing-r2-applied.md`): R2 промоут (мы — фактический 2-й потребитель) + 2 граблины.

**Сессия 2026-06-19 — хаб игр + игра v2 «Угадай по картинке» + 3 фикса (всё на ПРОДЕ).** Заявка владельца: сделать вторую игру (v1 не убирать), список игр для выбора, во v2 — выверенные вопросы с картинками (чужие можно, со ссылкой-источником). **Сделано в 2 PR по плану** (`.claude/plans` апрувнут): **PR #124 (каркас)** — `lib/quizGames.ts` (каталог игр, конфиг как cultureSections); `/igra`(+tt) → хаб-выбор (`GamesHubView`, карточки), игра играется на `/igra/[game]`(+tt) → `QuizView(game)`, неизвестный slug → notFound; поля `game`/`image`/`imageSource` в `quiz-questions`(+`_v`) + `game` в `quiz_results` (раздельная статистика); миграция `20260619_080000` (имена из `payload migrate:create`, backfill старых вопросов → `sabantuy`; накат `apply-migration --ref` ДО мержа, ask-гейт, потом deploy-prod --ref main по #017). **PR #125 (контент)** — `seedQuizImages.ts`: 8 вопросов «угадай по картинке» (köräш/чак-чак/эчпочмак/«разбей горшок»/концерт/собор Малмыжа/Вятка/калфак), RU+TT (tt черновик), факты выверены (ru.wikipedia, поле `source`), картинки **свободные Commons** (CC0/CC-BY/CC-BY-SA/PD) через `web/scripts/process-quiz-images.mjs` → `web/public/quiz/*.{jpg,webp}` (коммитятся, как декор), **каждая визуально проверена** (sharp→Read; первый köräш-кадр отбракован — не борьба → заменён); `imageSource` = страница Commons (атрибуция под фото в фидбэке, не спойлит); `seed-quiz-images.yml`. **Прод-смоук:** хаб 2 карточки; `/igra/sabantuy` (16 q) + `/igra/kartinki` (8 q) + tt = 200; битый slug → 404; `/api/quiz-results`→403 (#015); картинки `/quiz/*.{jpg,webp}`→200; backfill 16 старых → sabantuy подтверждён. **Также на проде в эту сессию:** LiveInternet `escape()` вместо encodeURIComponent (PR #118 — но реальная причина «не видно» = рунет-анти-баннер режет counter.yadro.ru); ISR `withRetry`+throw-vs-404 (PR #120, лекарство к pool #040 → brain #121); `<html lang>` на /tt после гидрации (PR #122); тизер игры на главной (PR #123). Заметки brain: #119 (анти-баннер LI), #121 (ISR-лекарство).

**Сессия 2026-06-18c — счётчики посещаемости + SEO/GEO (PR #111, на ПРОДЕ).** Заявка владельца: счётчики под Рунет (лёгкие) + SEO с упором на выдачу в нейросетях. **Счётчики:** `components/Analytics.tsx` — Яндекс.Метрика (`NEXT_PUBLIC_YANDEX_METRICA_ID`) + LiveInternet (`NEXT_PUBLIC_LIVEINTERNET`) через `next/script` (afterInteractive/lazyOnload, вне критич. пути). **Env-gated:** пусто → не рендерится (dev/до-настройки трафик не пачкаем). `deploy-prod.yml` прокидывает `vars.*` в сборку; `.env.example` описан. `ConsentNotice.tsx` — плашка cookie/аналитика (152-ФЗ + ToS Яндекса), не блокирует, localStorage, показывается только если счётчики вкл. Клауза веб-аналитики добавлена в шаблон Политики ПДн (`seedFromWp.ts`). ⚠️ **Top.Mail.ru исключён** — с 27.03.2026 туда нельзя добавлять новые сайты (проверено вебом). **SEO/GEO (всё серверное, 0 JS):** `lib/jsonLd.ts` + `components/JsonLd.tsx` — WebSite+Organization (layout), Festival/Event (главная: 4 июля 2026, Малмыж), **FAQPage** (4 выверенных Q&A — для цитирования ИИ), BreadcrumbList (страницы), RU/TT. `app/llms.txt/route.ts` (стандарт llmstxt.org, факты+ссылки из `getCultureSections`). `robots.ts` — явные allow ИИ-ботам (GPTBot/ClaudeBot/PerplexityBot/Google-Extended/YandexBot…) + Host + sitemap. Мета: canonical+hreflang ru↔tt (layout + pageMeta), og:alternateLocale. `lib/site.ts` + факты в `lib/festival.ts` (единый источник; фикс устаревшего fallback-URL layout). **Без миграций.** **Проверено preview:** JSON-LD на `/`,`/tt`,`/faq` (все типы, дата 2026-07-04, tt-локаль), llms.txt 200, robots с ботами, счётчики off без env, с тест-env — `window.ym` инициализируется, LI грузится, плашка показывается/закрывается, 0 ошибок консоли. Гейт зелёный (typecheck+lint+build 17/17, First Load JS не вырос). **Прод-смоук вживую** (node fetch, после ретраев — G8): JSON-LD Festival/FAQPage на проде, llms.txt 200, robots с ИИ-ботами+Host, аналитика спит. **Действия владельца** (в PENDING): завести Метрику/LI → GitHub Variables → деплой включит; опубликовать клаузу аналитики на живой `/privacy` (owner-gated). **Инж.-хвост (PENDING):** `<html lang="ru">` в layout жёсткий — на `/tt` декларирует ru (hreflang компенсирует, но lang некорректен); фикс — locale-aware lang (нужен раздельный layout/сегмент).

**Сессия 2026-06-18b — счётчик/статистика игры (PR #109, на ПРОДЕ).** Заявка владельца: на странице игры показать число прошедших + распределение результатов + средний балл, обезличенно. **Коллекция** `quiz-results` (`score`/`total`, без PII; #015: `create=anyone`, `read/update/delete=adminOrEditor` — итог отдаёт сервер агрегатом, `GET /api/quiz-results`→403). **Хук** `rateLimitQuizResult` (образец `PollVotes`): rate-limit по IP + валидация целостности (целые, `0 ≤ score ≤ total`, `1 ≤ total ≤ 500`; подделка `score>total`→400). **Сервер** (`QuizView.getQuizStats`) агрегирует count-бакетами по баллам 0..N (как итоги опроса), учитывает только текущий набор (`total === questions.length`), ISR 60. **Клиент** (`QuizGame`) шлёт результат при завершении — один POST на браузер для набора (флаг localStorage `quiz-sent:<total>`), оптимистичный `bumpStats` (игрок сразу видит себя). **Витрина** `QuizStats.tsx` (презентационный, ru-плюрализация «посетитель/посетителя/посетителей», tt — «кунак»). **Миграция `20260618_160000`** (таблица `quiz_results`, push-inspect; verify down→up→pg_dump diff 1:1 — единственная разница `\restrict`-nonce pg_dump 17, см. ниже). i18n `game.stats.*` + CSS `.quiz-stats*`. **Прод-роллаут (#017):** apply-migration `--ref feat/quiz-stats-counter` → merge #109 → auto-deploy упал на migration-guard (ожидаемо) → `deploy-prod --ref main` зелёный (build+smoke `/`+`/map`+`/admin`). **Прод-смоук вживую** (node fetch, домен ответил): `/igra`+`/tt/igra`→200, витрина с пустым состоянием («будьте первым» / «беренче булыгыз» — на проде 0 результатов), read→403. Локально preview: RU/TT рендер с данными (12 игроков, 9,7 из 16, распределение), POST→201, подделка→400, ноль ошибок консоли. Гейт зелёный (typecheck+lint+build 16/16). **Замечание (кандидат в GOTCHAS, письмо brain):** `pg_dump` 17 в каждый дамп вставляет `\restrict <random>`/`\unrestrict <random>` — при verify «down→up→pg_dump diff» эти 2 строки всегда различаются (ложный дифф); фильтровать `grep -vE '^\\(un)?restrict'`.

**Сессия 2026-06-18 — игра-угадайка (PR #106, на ПРОДЕ).** По директиве brain (recommend): интерактивная познавательная викторина. Формат — один гибкий механизм (выбор ответа) под все стили из брифа («правда/миф», «переведи слово» — через поле `format`). **Коллекция** `quiz-questions` (versioned+drafts, read=published, write=adminOrEditor #015): `prompt`/`options[text(localized)+correct]`/`explanation`/`hint` RU/TT, `source` (не-localized, поле фактчека), `theme`/`format`/`difficulty`, `key` (идемпотентный сид). Валидация «ровно один верный». **Фронт:** `_views/QuizView` (сервер, только published) + `/igra`+`/tt/igra` + клиентский `QuizGame` (механика, прогресс в localStorage, Web Share, перемешивание по клику «Начать» — не в рендере, иначе hydration mismatch). Звания по доле верных. Навигация+sitemap+i18n+CSS. **Миграция `20260618_140000`** (8 таблиц versioned+localized-array + 9 enum'ов, push-inspect; verify down→up→pg_dump diff 1:1, 186/186). **Сид `seedQuiz.ts`** — 16 вопросов RU+TT, факты выверены по ru.wikipedia/culture.ru/tatarica (2026-06-18), у каждого `source`. `seed-quiz.yml`. **Прод-роллаут:** apply-migration (--ref) → merge → deploy (guard→dispatch) → seed-quiz → смоук `/igra`+`/tt/igra`=200, 16/16 опубликовано. Проверено локально через preview (обе локали, интро→16 вопросов→результат, фидбэк с источником, рекорд localStorage, ноль ошибок консоли). **Письмо brain** (`mailbox/to-brain/2026-06-18-quiz-game-shipped.md`): отчёт + переносимая находка (versioned+localized-array миграция, tt-update по row-id, усечение имён индексов 63 симв., shuffle-on-click vs hydration). Владелец выбрал «искать факты самим» → deep-research у brain не заказывали. tt — черновик (вычитка в PENDING).

**Сессия 2026-06-13 — карточка №4 (PR #100/#102/#103, на проде) + визуальная проверка через Chrome-расширение.** Владелец прислал ~16 кадров, встроено: (1) **фича-ряд «Что вас ждёт»** — 6 карточек получили фото-обложки 4:3 (köräş/скачки/столб/кухня/концерт/дети), раскладка 2→3 кол. как культ-хаб (`sabantuyFeatures.cover` + `FeatureRow coverLayout` + `.feature-row--cover`); (2) **силуэт Малмыжа в подвале** — `footer-skyline.png` (белый фон AI-кадра вырезан в прозрачность порогом яркости, перекрашен в кремовый), лентой во всю ширину над навигацией (это и есть «город внизу», которого ждал владелец); (3) **миниатюры расписания** — `CATEGORY_COVER` (concert→feat-concert, sport→feat-koresh, food→feat-cuisine, kids→feat-kids, ceremony→cat-ceremony, crafts→card-podvorya), `ScheduleList` flex (миниатюра+тело); (4) **CTA-фикс** — `ctaPhoto` фолбэк на `page-maydan-lg` → больше не падает на малиновый `section--crimson` (мерцал красным при ISR-перегенерации без галерейного фото); (5) **крошка шапки** (PR #100) — `.page-hero .section-inner.narrow` flex-колонка (крошка/медальон/eyebrow/заголовок налезали — все inline-flex). Проверено вживую через Chrome-расширение: фича-обложки, силуэт, миниатюры, CTA на фото, крошка не налезает. **Наблюдение:** расписание на главной иногда показывает пустое состояние (ISR-перегенерация в момент сбоя `getPublishedEvents` → null → кэшируется ~60с, само выправляется) — см. PENDING.

**Сессия 2026-06-13 — декор-волна 2 (PR #94, на проде) + промт-карта №3.** По двум пожеланиям владельца: (1) **red→green** — токен `--hero-overlay` (вуаль ВСЕХ фото-шапок + главного героя + секций `.section--photo`) уходил в малиновый к низу → теперь изумрудный по всей высоте (это и были «красные затемнения везде»); pageDecor де-малинен (istoriya→gold, kuhnya→green); faq-обложка культ-хаба → зелёная; OG-вуаль перегенерирована зелёной. (2) **рисованные SVG-сцены долой** — `SceneIllustration` (11 сцен кодом, в т.ч. «город»/road, что не нравился) убран и удалён; страницы с фото → чистая фото-шапка; без фото (faq/kontakty/privacy) → мотив-медальон до прихода фото. Прод-смоук: `/narody` `page-hero--photo` без `page-hero-scene`; CSS-вуаль `rgba(7,38,23,0.74)`, малинового `122,18,40` нет; `/faq` медальон; `/og.jpg` 200 зелёный. **Зачистка:** ветки (prune — на origin только main), устаревший комментарий `deploy-prod.yml` (стр.10) поправлен. **Промт-карта №3** (`docs/design/PROMPTS_2026-06-13_wave2.md`): A — фото-шапки faq/kontakty (21:9); B — рисованный декор (орнамент-лента, угловые виньетки, бесшовная плитка, фолк-панорама); C — шамаиль-панель для privacy/о-фестивале. **Ждём картинки от владельца** → встрою (вырезка декора + фото-шапки).

**Сессия 2026-06-13 — обложки культ-разделов 4:3 (PR #90, на проде).** Заполнен последний слот `IMAGE_PIPELINE.md`: 7 из 8 карточек хаба «Традиции и культура» на главной получили фото-обложку 4:3 — кроп ТЕХ ЖЕ accepted-исходников, что и шапки соответствующих страниц (карточка = мини-превью своей страницы): narody/podvorya/istoriya(south)/maydan/detskiy/kuhnya/doroga. `faq` без тематического фото → мотив-иконка на фирменном градиенте (единый каркас → высоты в сетке совпадают). Конвейер: `process-decor.mjs` проход `COVERS` (sharp cover-кроп 4:3, **mozjpeg q74, JPG-only** — webp на детализированных кадрах тяжелее mozjpeg; размеры 768+480 под `srcset`) → `web/public/decor/card-*.jpg` (14 файлов, 25–130 КБ). Код: `cultureSections.cover` (slug, локаль-независим) → `FeatureCard` режим `coverLayout` (фото-обложка или градиент-иконка; фича-ряд «что вас ждёт» не затронут) → `festive.css` `.feature-card--cover`/`.feature-cover(--icon)`/`-body` + hover-зум (гасится reduced-motion). Гейт typecheck+lint+build(14/14) зелёный. **Прод-смоук вживую** (боевой домен ответил, G8 не помешал): `/` и `/tt` — 7× cover-`srcset` + faq icon-band; обложки 768+480 → 200. **Грабля кропа:** контактный лист sharp→PNG→Read (G44) перед выбором — выбирал кадр глазами, не по имени файла.

**Сессия 2026-06-12/13 — карточка №2 (PR #88).** Ещё 18 кадров от владельца → принято 4: narody (ряд костюмов на раме), podvorya (изба с наличниками+полотенца), detskiy-maydan (лошадки-качалки+подушки), kak-dobratsya (дорога с тюльпанным указателем, река, городок — прямо Малмыж на Вятке). Забраковано 3 квадрата (крупные лица детей/деда; сказочное существо). Запас пополнен (`design/accepted/spare/`: 2-е костюмы, тёмная изба, lucid-двор, зелёные лошадки, seedream-квадрат 2048, 3 дорожных варианта). `process-decor.mjs` теперь ищет исходники в incoming/+accepted/ (перезапускаем). Прод-смоук: 4 новые страницы + /tt/narody PHOTO, ассеты 200. **Грабля сессии:** `next build` при работающем dev-сервере перезатирает `.next` → dev отдаёт 500 MODULE_NOT_FOUND (vendor-chunks); лечение `rm -rf .next` + рестарт dev. Фото-слоты заняты 8/11; без фото остались faq/kontakty/privacy (служебные, осознанно на фолбэке).

**Сессия 2026-06-12 — AI-картинки приняты и на проде (PR #86).** Владелец накидал 11 кадров в `design/incoming/` → приёмка: 4 принято (панорама майдана 3168px, плуг+полотенце, кинематографичный казан, столб с призами), 7 в запас (`design/accepted/spare/` — телега, столы, 3 квадрата 1024 на будущие обложки 4:3). Конвейер: `web/scripts/process-decor.mjs` (sharp, кадр 21:9, у истории `position:'south'` — attention резал плуг; WebP q80+JPG, lg+960) → `web/public/decor/` (16 файлов, ≤270 КБ). Код: `pageDecor.photo` → PageView ставит `.page-hero--photo` + инлайн-vars `--hero-photo(-sm)` c `image-set()` → festive.css (фон cover, вуаль ислими+`--hero-overlay`, светлый текст, swap 960/lg на 900px). SVG-сцены сохранены поверх (гибрид). Страницы без фото — прежний фолбэк. Прод-смоук: 5 страниц PHOTO (вкл. /tt/kuhnya), /faq plain, 4 ассета 200. Заняты слоты: kuhnya, maydan, istoriya-sabantuya, o-sabantuy.

**Сессия 2026-06-11 — официальная дата + knip (#036) + плуг + AI-image-конвейер.** (1) **#80** — **дата 4 июля 2026 везде**: `seedDemoProgram.DAY` → `2026-07-04` (прогнан `seed-program.yml force=1`, 12 demo-событий передвинуты), предупреждение ru/tt переформулировано (дата настоящая, предварительная только программа; `DEMO_SCHEDULE` остался `true`), FAQ «Когда и где» ru/tt — конкретная дата (`seed-culture force=1` + `seed-tatar` прогнаны). Прод-смоук через SSH (внешний доступ с этой машины фильтруется edge — G8): дата в API и текстах на `/`, `/tt`, `/faq`, `/tt/faq`. (2) **#81** — **knip внедрён** (suggest #036): `pnpm -C web deadcode`, конфиг `web/knip.json` (сиды=entry, sw.js=ignore); вычищено: `access/authenticated.ts`, dep `@payloadcms/ui`, 8 мёртвых экспортов, 5 un-export. Ack brain — #82. (3) **#83** — сцена **«плуг» перерисована** узнаваемо (сабан в борозде: грядиль+кольцо, две рукояти, лемех, отвал; verify G44 sharp→PNG→Read); прод-смоук `/istoriya-sabantuya` ок. (4) **#84** — **конвейер AI-картинок** заведён: `docs/design/IMAGE_PIPELINE.md` (процесс 4 шага, правила генерации, единый стиль-блок, карта слотов, тех-заметки sharp→WebP→`web/public/decor/`), папка обмена `design/incoming/` (gitignored). **Владельцу выдана карточка №1** (3 промта под ГигаЧат/Кандинский, русские, панорамные): `page-kuhnya.jpg`, `page-maydan.jpg`, `page-istoriya.jpg` — фоны шапок страниц (гибрид: AI-фон + наша SVG-эмблема поверх). ⚠️ Промты №2/№3 переписаны без упоминания людей/лиц — фильтр ГигаЧата режет такие слова (№1 прошёл сразу).

## Следующий шаг

**▶ ГЛАВНОЕ: задача #4 (VK ID вход) ждёт владельца (owner-gated).** Владелец по гайду [`docs/ops/vk-id-setup.md`](ops/vk-id-setup.md) регистрирует VK-приложение → передаёт **App ID в чат** + **Защищённый ключ в GitHub Secret `VK_CLIENT_SECRET`**. Дальше я делаю **PR #5**: коллекция `visitors` (vkId/name/avatarUrl) + своя сессия-cookie (подпись `PAYLOAD_SECRET`, отдельно от стафф-входа `/admin`) + `/api/auth/vk/authorize`+`/callback` (OAuth 2.1, PKCE+state, redirect `…/api/auth/vk/callback`) + кнопка «Войти через VK» в шапке (рядом с `LoginControl`) + закрепление UGC за аккаунтом (ownerHash от `vk:<id>` ИЛИ relation `visitor`) + имя/аватар на карточках. Анонимный путь остаётся. **Реальный OAuth-раунд проверяется только с боевым VK-приложением** → код не пишем спекулятивно, ждём ключи (как с S3).

**Задачи #2 (PWA-установка) и #3 (управление своим UGC + админ-модерация) — ✅ на проде** (PR #166/#167/#168). Желательное/owner-действия по ленте/эфиру (без изменений):

1. **(Желательно) Первый реальный пост владельца с телефона** — `/lenta` → «Поделиться кадром» → «Снять на камеру» или файл → опубликовать → карточка ≤30с. Проверить заодно: на своей публикации появляется бейдж «Ваше» + 🗑 (удаление), у комментов — «Изменить»/«Удалить».
2. **(Owner, в день эфира) Включить трансляцию** — `/admin → Прямой эфир` → вставить ссылку на VK-видео/эфир + «Эфир идёт» → плеер на `/efir`. ✅ Механизм на проде (#162).
3. **(Опц.) Модератор онлайн в день праздника** — постмодерация «виден сразу»; жалоба→авто-скрытие на 3-м репорте; **в режиме «Редактирование» персонал теперь удаляет любую публикацию/коммент прямо в ленте** (🗑/«Удалить»), не заходя в `/admin`.

**Эксплуатация:** ключ S3 можно ротировать тем же воркфлоу `apply-s3-keys.yml` (обновить секреты GitHub → запустить). Стоимость S3 мониторить (egress — главная переменная; уже минимизирован: даунскейл фото, видео click-to-play без автоплея, лимит длины).

**Хвосты (owner-gated, PENDING):** таймлайн реальных событий (→ `DEMO_SCHEDULE=false`), SMTP-секреты, текст «Политики ПДн», **вычитка tt носителем**, план территории карты, **пиковый probe-замер в день 4 июля 2026**.

## Не забудь

Отложенное живёт в **[`docs/PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md)** (формат #033, re-триаж на `/start`). Ключевое сейчас: AI-картинки от владельца (карточка №1), реальная программа к сезону (дата ✅ 4 июля), SMTP-секреты, вычитка tt носителем, план территории для карты, пиковый probe-замер **в день праздника 4 июля 2026**.

---

> 📌 **Предыдущая сессия (2026-06-07c):** mailbox-сессия: 2 recommend-письма brain обработаны, ответы на проде (PR #77). (1) **G38 (Payload importMap stale)** — проверил наш build-путь, **НЕ задеты**: `web/src/app/(payload)/admin/importMap.js` трекается в git (не gitignored — предпосылка G38 не выполняется), ноль проектных кастомных admin-компонентов (в `admin:` нет `components:`; все 24 записи — `@payloadcms/*`), свежий `generate:importmap` → `No new imports found` + пустой diff. Пинг brain `mailbox/to-brain/2026-06-07-g38-importmap-checked-clean.md`. (2) **Probe #020 (консолидация серверов, Бокс 1)** — read-only замер прод-бокса (myjino tiny, 1.5 ГБ), **базовая нагрузка**: next-server RSS **≈230 МБ**, Postgres-кластер **≈50–60 МБ**, БД `sabantuy` **11 МБ** (PII~0: registrations=0, raffle_entries=0, subscribers=1), диск 9.8 ГБ занято 35%, load avg ~0. ⚠️ **Сезонный пик не пойман** — главный недостающий замер (фестиваль июнь–июль); постараться снять в день праздника и дослать. Отчёт `mailbox/to-brain/2026-06-07-server-probe-myjino-tiny.md`. SSH `ssh sabantuy` сработал в эту сессию (G8 не помешал). (3) **Консультация по бэклогу:** 7 «идей на потом» владельца — **все уже в `docs/ideas/site-enhancements.md`** (I7 фильтр карты, D2 «сейчас на майдане», D3 «Новинка-2026», I8 UGC-фотостена, R2 артисты, R7 спонсоры, I9 OG); дубликаты не заводил. Открыто: вынести per-event OG (динамические соц-превью по событиям, сейчас подпункт I9) отдельной строкой **I12** — предложено, ждёт OK владельца, НЕ внесено.

> 📌 **Пред-предыдущая сессия (2026-06-07b):** директива brain «page-imagery» (ни одной сухой страницы) обработана, R1+R2 на ПРОДЕ. (PD1) **#68** — **R1 контент-aware орнамент-слой** прозовых страниц (`PageView`): декоративная шапка (мотив-медальон по смыслу + орнамент-текстура `pattern-petals` + eyebrow + `heading-brush`) + хвостовой vine-divider; конфиг `lib/pageDecor.ts` (slug→{иконка,акцент,eyebrow,photo}, ru/tt); CSS `.page-hero` + акценты green/crimson/gold/tint. (PD2→PD3) **#70 → #72** — **R2 реальные фото в шапках** (фон из featured-альбома) был выкачен, затем **заменён R3** по выбору владельца: фото generic-альбома не «строго по смыслу» (на «Кухне» — общий кадр, не еда). (PD3) **#72** — **R3 тематические фолк-иллюстрации, нарисованные кодом** (`SceneIllustration.tsx`, 11 SVG-сцен: борьба көрәш с кушаком, казан+чак-чак, хоровод народов, нац. дом, плуг+колос, змей+мяч, дорога+указатель, пузыри-FAQ, конверт+метка, документ+щит, солнце+тюльпаны). `pageDecor.scene: SceneName|null`; крупно в шапке вместо медальона (медальон — fallback). Убран `queryHeroPhoto`+`.page-hero--photo`. Реальные фото остались в Галерее/на главной. Прод-смоук: `page-hero-scene` на всех страницах, фото-вариант ушёл. **Политика источников фото согласована с владельцем** (разрешены все тиры: реальные/AI/CC/сток; приоритет реальные→AI→CC; сток — крайний случай). Ack brain — `mailbox/to-brain/2026-06-07-page-imagery-ack-policy-and-r1-done.md` (PR #69). ⚠️ **AI-фото-генерация недоступна в сессии** (нет image-gen tool, в реестре коннекторов нет; только Gamma=презентации) → R3 реализует тир-2 «своя графика без копирайта» **кодом**. **Приём verify «вслепую»:** скриншот превью в этом окружении не работает → SVG отрисованы в PNG через `sharp`+`Read` (увидел все 11). Кандидат в pool/GOTCHAS (письмо brain).

> 📌 **Предыдущая сессия (2026-06-06→07):** автономия + добивка фич/SEO/a11y, всё на ПРОДЕ. (1) **#41** — **#027 gate-replaced autonomy** (`.claude/settings.json` `auto` + ярусные гейты + ack brain). (2) **#42** — **I10 PWA + офлайн** (manifest/SW/иконки/`/offline`; navigations network-first, статика cache-first). (3) **#43** — **D1 лепестки над героем** + V5 ✅. (4) **#45** — **I5 регистрация на состязания** (`competitionType` в `Registrations`; **миграция `20260606_180000`** накачена на прод по потоку #017). (5) **#46/#48/#49** — **SEO sitemap-сага**: покрытие (`/map`,`/gallery`,альбомы) → резистентность (per-collection try/catch) → **корень: `force-dynamic`** (sitemap пререндерился в CI против пустой БД → 3 URL; теперь 28). (6) **#47** — фикс ложно-нулевого verify в `apply-migration.yml` (через спавн-сессию). (7) **#51** — **a11y лайтбокса** (focus-restoration + focus-trap). (8) **#53** — **I6 подписка на анонс** (`Subscribers`: email-unique-дедуп + consent 152-ФЗ + #015; форма на главной; rate-limit + письмо-подтверждение; **миграция `20260607_010000`** накачена на прод, verify 1:1; прод-смоук: read→403, consent-гейт→400). (9) **#55** — **I4 розыгрыш призов** (`Raffle` + `RaffleEntry`: ФИО/телефон/consent, #015, winner field-locked; рандом-победитель галкой `drawNow` в админке → хук `drawRaffleWinner`; форма на главной при открытом розыгрыше; rate-limit + gate `enforceRaffleOpen`; **миграция `20260607_020000`** — 2 таблицы + перекрёстные FK, накачена на прод, verify 1:1). (10) **#58** — **env-фикс**: `output:standalone` мутировал локальный node_modules (НЕ Defender — антивируса нет) → теперь под флагом `STANDALONE_BUILD=1` (CI), локальные сборки стабильны. (11) **#60** — **I11 путевой переключатель RU/TT (механизм)**: ru на `/`, tt на `/tt`; `lib/i18n.ts` (словарь UI ~90 строк, татарский черновик) + `localeHref` + `SiteChrome` (клиентский, локаль из usePathname, ISR цел) + `_views/*` (общие тела) + `/tt/*` обёртки + payload.find({locale}) с fallback ru + sitemap hreflang. Прод: /tt→200, татарский UI, тумблер, 28 hreflang. Письма brain: #44 (pnpm-стор), #50 (sitemap-пререндер), ack #027.

> 🌐 **I11 — на проде (механизм + контент-черновик).** Готово и живо: переключатель RU/TT, путевой роутинг (/ ru, /tt tt), UI-строки tt (`lib/i18n.ts`), sitemap hreflang, **+ контент tt**: 8 культ-разделов (title+body на татарском) + intro карты + заголовки o-sabantuy/kontakty/privacy — залито сидом `seedTatar.ts` через `seed-tatar.yml` (прод-смоук /tt/* зелёный; `/tt/narody`/`/tt/maydan` отдают татарский title+body; ru цел). **⚠️ Татарский — ЧЕРНОВИК (не носитель), нужна вычитка** — правится в `seedTatar.ts`/`lib/i18n.ts` или прямо в `/admin` (ru/tt-селектор). **lib-конфиги ✅ локализованы (PR #64)** — категории, легенда карты, культ-хаб, фича-ряд, опрос на tt (getter'ы с locale; ru-экспорты сохранены для админки). Теперь **/tt полностью татарский**: UI + контент культ-разделов + конфиги. **✅ ДОБИТО 2026-06-07b (PR #74):** tt-тела o-sabantuy/kontakty/privacy + tt-галерея (title/description) залиты на прод (`seedTatar.ts` → `seed-tatar.yml`). **Осталось:** опц. демо-Events (заменит владелец) + **⚠️ вычитка носителем** (весь tt — мой черновик; правится в `seedTatar.ts`/`/admin`, потом `gh workflow run seed-tatar.yml --ref main`). Ранее (2026-06-06): #31/#32 культ-разделы, #33 галерея, #34 отсчёт+демо, #36 «Моя программа», #37 опрос+миграция, #39 фотолента+OG.
**Веха:** M1–M3 ✅ · season-MVP ✅ · RU/TT (#17) · редизайн (#21) · SMTP (#24) · фото (#28) · культ-разделы+галерея+отсчёт+демо+«Моя программа»+опрос+OG ✅ · **#027 автономия · I10 PWA · D1 лепестки · I5 состязания+миграция · sitemap force-dynamic · лайтбокс-a11y · I6 подписка+миграция · I4 розыгрыш+миграция ✅ — всё на ПРОДЕ**. **План улучшений (`docs/ideas/site-enhancements.md`) ИСЧЕРПАН** — все строимые фичи сделаны. Остаток **ждёт ТОЛЬКО владельца:** реальные дата+события (→ `DEMO_SCHEDULE=false` + чистка `demo-*`), артисты/логотипы (R2/R7 — контент), SMTP-секреты в прод-env (чтобы письма I6/заявок пошли реально), текст «Политики ПДн», TT-переводы; owner-gated I7 (нужен план территории для фильтра карты), I11 RU/TT-тумблер (нужны реальные переводы). **Использование розыгрыша (I4):** орг создаёт `Raffle` в /admin, включает «приём заявок», в день праздника ставит галку «провести розыгрыш» → система выбирает победителя.
> ✅ **I4 внешний endpoint-смоук добит** (2026-06-07, после восстановления связи): прод `home`→200, `GET /api/raffle` (anon, read=anyone)→200, `GET /api/raffle-entries` (anon, #015)→**403**, `POST /api/raffle-entries` без consent→**400**. Миграция `20260607_020000` накачена (CREATE TABLE ×2 + записано в payload_migrations). I4 полностью верифицирован.

> **✅ Канонический URL исправлен (2026-06-06):** `vars.NEXT_PUBLIC_SERVER_URL` был застрявший `http://127.0.0.1:3000` (плейсхолдер до-TLS) → обновлён на боевой домен **`https://xn--80aaac1aqpgcf4bqn1j.xn--p1ai`** (= `сабантуймалмыж.рф`, отдаётся по HTTPS 200) + ре-деплой. Это URL, который пекётся в бандл при сборке: чинит `og:image`/`og:url` (соц-шеринг) и CORS-allowlist (`payload.config`). ⚠️ Комментарий в `deploy-prod.yml` (стр.10 «пока http://127.0.0.1:3000») теперь устарел — косметика. **Punycode-форма** (ASCII) намеренно — кириллица в CI-bash бьётся (G11).

> **⚠️ Демо-данные на проде (PR #34):** программа и дата — ПРЕДВАРИТЕЛЬНЫЕ (микс прошлых лет), показываются с заметным предупреждением (`FestivalNotice`, флаг `web/src/lib/festival.ts → DEMO_SCHEDULE = true`). Обратный отсчёт целится в ближайшее будущее событие. **Когда организаторы дадут реальную дату/программу:** (1) `DEMO_SCHEDULE = false` — предупреждение исчезнет; (2) удалить события со slug `demo-*` (в /admin или `DELETE FROM events WHERE slug LIKE 'demo-%'` через прод-БД). Сид демо: `seedDemoProgram.ts` / `seed-program.yml`.

> **Паттерн наполнения (этот этап):** разделы — **контент-страницы `Pages`** (без миграций) сидом `seedCulture.ts`; конфиг карточек хаба+подвала — `lib/cultureSections.tsx` (DRY). События — `seedDemoProgram.ts` (коллекция Events, без миграций). На прод — workflows `seed-culture.yml` / `seed-program.yml` (`--ref main`) **после** деплоя кода. Локальный preview: PG поднимать через `pg_ctl start -D "C:\Program Files\PostgreSQL\17\data"` если служба стоит (Start-Service требует админа); галерея без реальных фото — `seedDemoGallery.ts` (dev-only); **screenshot/viewport в превью-окружении вырожденный (0-width)** → верификация через inspect/HTML/eval (числа отсчёта, классы, computed-стили), не скриншотом.

> ✅ **РЕШЕНО (PR #58) — «node_modules ломается между сборками» это был НЕ Defender.** Диагноз исправлен: антивируса на машине нет (`SecurityCenter2` пуст, службы `WinDefend` нет). Настоящая причина — **`output: 'standalone'`** делал `outputFileTracing`, который мутировал `node_modules` (удалял `next/dist/client/components/builtin/*`) → следующая локальная сборка/`typecheck`/`lint` падала `Cannot find module … global-not-found`. CI не страдал (свежий install на каждую сборку). **Фикс:** `output:'standalone'` теперь под флагом `STANDALONE_BUILD=1` (ставит только `deploy-prod.yml`); локальный `next build` обычный, node_modules не портит → повторные сборки без реинстолла (проверено build→build→typecheck зелёные; CI-деплой standalone тоже зелёный). [Устаревшая заметка ниже — историческая:]
>
> 🗑️ ~~Windows Defender ест `node_modules` → нужен `Add-MpPreference`-exclusion.~~ **ОШИБОЧНЫЙ диагноз** — антивируса на машине нет; реальная причина и фикс — в ✅-блоке выше (PR #58). Exclusion НЕ нужен.

> ⚙️ **sitemap теперь `force-dynamic` (PR #49).** `/sitemap.xml` НЕ пререндерить в сборке: Next делал это в CI против пустой build-БД → запекал вырожденный sitemap (3 URL), ISR держал час. `export const dynamic = 'force-dynamic'` строит в рантайме против прод-БД (28 URL). **Урок (письмо brain #50):** любой data-driven static/ISR route (sitemap/RSS/листинги) в CI берёт пустую build-БД → `force-dynamic`. Не ставить обратно `revalidate` на sitemap.

> 🔧 **#017-миграции — рабочий поток подтверждён вживую (I5, PR #45):** `gh workflow run apply-migration.yml --ref <feature-branch> -f migration=<ts>` (накат ДО мержа; `--ref` обязателен, G28) → merge → авто-деплой падает на migration-guard ИЛИ на Docker-pull-флейке «Initialize containers» (транзиент Docker Hub, не код) → `gh workflow run deploy-prod.yml --ref main`. ⚠️ **Verify в `apply-migration.yml` чинён (#47), но раньше печатал ложный `events.venue: 0`** — это `information_schema.columns` под прод-ролью БД, НЕ отсутствие колонки (прод-API/SQL подтверждали наличие). Доверять `psql -f` (ON_ERROR_STOP) + записи в `payload_migrations`, не information_schema.

> ✅ **brain sync прошёл в эту сессию** (`Already up to date`). Входящие: 1 mandate (#027 — применён, ack отправлен) + informational. Исходящие письма: #44 (pnpm-стор-граблю), #50 (sitemap-пререндер против пустой CI-БД — GONBA вероятно задета), ack #027. **Урок про спавн:** один чип-задача (`apply-migration verify`) расплодилась в 3 параллельные сессии вокруг одного CI-файла → дубли коммитов + путаница с локальным main. Спавнить фоновые задачи по смежным темам осторожно.

## Сделано

- **M1** (PR #1): каркас Next 15 + Payload 3.75.0 + Postgres, коллекции, доступ #015. Проверен **вживую**: admin создан, расписание видно на главной.
- **M2** (PR #3, **смержён в main**): публичная регистрация.
  - Страница события `/events/[slug]` (детали + форма заявки, когда `registrationEnabled`).
  - Клиентская форма → `POST /api/registrations`, галка согласия 152-ФЗ со ссылкой на политику.
  - Маршрут статических страниц `/[slug]` (рендер `Pages.content` через `RichText`).
  - Страница «Политика обработки ПДн» (slug `privacy`) — засеяна в `Pages`, ссылка в подвале.
  - **Hardening #015**: поля `status`/`source` заявки закрыты field-level access от подмены анонимом (`adminOrEditorField`).
  - **Серверный gate регистрации** — `beforeValidate`-хук `enforceRegistrationOpen`: анонимный `POST /api/registrations` отклоняется (400), если событие не `published` или `registrationEnabled !== true`; персонал не ограничен (телефонные заявки). Закрыл замечание ревью (прямой POST обходил UI-гейт). E2e через node: open→201, closed→400, bogus id→400.
  - **Email-уведомление** организатору — `afterChange`-хук `notifyOrganizer` (адрес из `ORGANIZER_EMAIL`; в dev пишется в консоль, на проде заработает с SMTP-адаптером).
  - Проверено зелёным: typecheck, lint, `next build`, и end-to-end через node (заявка / защита #015 / gate / отказ без согласия / public-read 403 / страницы рендерятся / email-хук логирует).
- **#016 read-аудит полей** (рекомендация brain) — ✅ прогнан, **чисто**. Публичный read только у `Media` (`anyone`: alt/caption — не PII) и published-доков `Events/Gallery/Pages` (черновики скрыты `authenticatedOrPublished`). `Registrations` read закрыт (#015), `Users` — `adminOrSelf` + `roles.update` заперт на admin. Чувствительных полей под публичным read нет; `admin.hidden`-as-security не обнаружено.
- **Пред-деплойное усиление** (PR #5, смержён):
  - **Rate-limit** публичного `POST /api/registrations` — хук `rateLimitRegistration` (beforeValidate, первым): 5 заявок / 10 мин на IP, только анонимы; персонал свободен. In-memory fixed-window (`lib/rateLimit.ts`) — для одно-инстансного VPS; при масштабе → Redis. IP из `X-Forwarded-For`/`X-Real-IP`.
  - **ISR** `revalidate=60` вместо `force-dynamic` на `/`, `/events/[slug]`, `/[slug]` + on-demand ревалидация (`revalidateEvent`/`revalidatePageDoc` на afterChange/afterDelete). `lib/safeRevalidate.ts` — перенос из GONBA. Серверный gate делает ISR безопасным.
  - **SEO**: `/sitemap.xml` (события+страницы, ревалидация 1ч) + `/robots.txt` (Disallow `/admin`,`/api`). ⚠️ оба в **app-root**, НЕ в route-group `(frontend)` (иначе robots не генерится — см. «Не делать»).
  - **Кастомная 404** (`not-found.tsx`).
  - E2e (node): robots/sitemap отдаются (6 url), rate-limit #1–5→400/#6→429 без строк в БД.
- **Полировка фронта** (PR #7, смержён): клиентский `ScheduleList` с чипами-фильтрами по категориям + категория-бейдж; hero-fallback (градиент) на детали события без изображения; `CATEGORY_LABELS` вынесен в `lib/categories.ts` (DRY). Render-smoke зелёный.
- **Письмо brain** (PR #6): `mailbox/to-brain/2026-06-03-nextjs-robots-route-group-gotcha.md` — грабля Next 15 (robots.ts в route-group не генерится), `kind=idea`/`suggest`, кандидат в GOTCHAS.
- **Season-MVP #2 «Карта фестиваля»** (PR `feat/festival-map`): глобал `festival-map` (план-картинка `planImage` → media + `intro` + массив `points{label,type,note}`, типы: сцена/еда/вход/парковка/туалеты/медпункт/другое), публичная страница `/map` (план + легенда по типам с иконками, graceful-заглушка без картинки), nav-ссылка «Карта» в шапке. read=anyone (не PII), update=adminOrEditor. Миграция `20260604_140000` (push-inspect; fresh-create+идемпотентность проверены drop→up→up на public). Зелёным: typecheck/lint/`next build` (9/9 страниц), /map SSR-рендер легенды на сэмпле. Asset плана — теперь просто upload, не блокер.
- **Season-MVP #1 «Программа фестиваля»** (этот PR): brain `recommend`/`urgency:high` к сезону.
  - Поле **`venue`** (площадка/сцена) в `Events` — аддитивно к версионируемой коллекции (G7; dev push накатил чисто, прод #017 — без `DROP`).
  - Клиентский `ScheduleList`: **«● Идёт сейчас / Далее»** (часы на клиенте, тик 60с — не зависит от ISR-staleness; `live`=start≤now≤end, `next`=ближайшее будущее), баннер **«Сейчас на фестивале»**, фильтр-чипы по **площадкам** (2-е измерение к категориям), 🎪 площадка в карточке.
  - Зелёным: typecheck, lint, `next build`, e2e node (login→create published event с venue+live-слот→venue персистится, главная рендерит площадку/фильтр→cleanup). now/next-бейджи — client-only (progressive enhancement, в SSR-HTML нет — by design).
  - **Ack brain** + scope-решение по карте (#2, отложена — нет asset) + **архитектурное решение по localization** — `mailbox/to-brain/2026-06-04-season-mvp-program-ack-and-localization-decision.md`.
- **Двуязычие RU/TT** (ветка `feat/localization-tt-ru`, **на ревью, не смержена**): `localization` в `payload.config` (locales ru+tt, default+fallback ru). Локализованы user-facing текстовые поля: Events (title/summary/location/venue/content), Pages (title/content), Gallery (title/description), Media (alt/caption), FestivalMap (intro). **Массивы НЕ локализованы** (gallery.photos.caption, map points.label/note) — иначе медиа-связи дублируются по локалям (by design, MVP).
  - **Миграция `20260604_160000`** (#017 push-inspect): 8 зеркальных `*_locales` таблиц + enum `_locales` + `published_locale`/`snapshot` на `_v`; локализованные колонки переезжают из базовых/версионных таблиц. DDL снят `pg_dump`'ом с dev-push на пустую БД. **Верифицирована строго:** `git stash` → push на пустую БД = прод-эквивалент baseline → применил миграцию через `psql` → `pg_dump` → дифф против каноничного push сошёлся 1:1 (колонки/PK/уникальные индексы/FK), идемпотентна при повторе. Зелёным: typecheck, lint, `next build` (compile+prerender 9/9; standalone-symlink EPERM — Windows-only, CI/Linux ок).
  - **Публичный язык-тумблер отложён** до реальных TT-переводов (решение владельца): пока tt пуст, фронт отдаёт ru (fallback); cookie-locale убил бы ISR. Админка уже даёт ru/tt-селектор. Когда появятся переводы — добавить тумблер + locale-aware чтение во фронт-`payload.find`.
- **Перенос контента со старого WP** (`feat/localization-tt-ru`, локально): idempotent seed-портер `web/src/seed/seedFromWp.ts` (`payload run`) тянет с `сабантуймалмыж.рф` через WP REST API (node fetch, UTF-8 — G11): страницы «О фестивале» (`/o-sabantuy`), «Контакты» (`/kontakty`), плейсхолдер «Политика ПДн» (`/privacy`); фотоотчёт «Сабантуй 2023» (9 реальных фото → альбом `/gallery/sabantuy-2023`); афиша+программа 2024 (постеры → `/gallery/afisha-programma-2024`). Даты сохранены. Кириллица в именах файлов транслитерируется (иначе дедуп схлопывал постеры). Новый фронт `/gallery` (список + `/gallery/[slug]`) + nav-ссылки «Галерея/О фестивале/Контакты». Проверено в preview: все страницы 200, фото грузятся, консоль чистая, API `?locale=ru|tt` с ru-fallback. **Контент засеян только локально** — на проде запустить seed после первого admin (или вводить вручную).
- **Контент со старого WP → ПРОД** (сессия 2026-06-05, PR #19 + фикс #20):
  - **`seed-prod.yml`** (`workflow_dispatch`) — прогон `seedFromWp` **на боксе** из CI: бокс клонирует репо (read-only deploy key `~/.ssh/github_sabantuy`), `pnpm install`, `payload run` с прод-`DATABASE_URL` (локальный `127.0.0.1`, без туннеля — seed легче `next build`, OOM нет). Идемпотентен. Прогон зелёный: 3 страницы, 2 альбома (9+4 фото), media=13, 44 файла в `shared/media`.
  - **dev-админ на проде НЕ создаётся** — `seedFromWp.ts` гейтит создание `admin@sabantuy.local` на `NODE_ENV !== 'production'`. Владелец заводит своего через create-first-user. (dev-пароль публичен в репо — нельзя на прод.)
  - **Персистентное медиа**: `deploy-prod.yml` симлинкает `<release>/public/media → ~/sabantuy/shared/media` → загрузки/seed переживают деплои (MVP-замена внешнему хранилищу ADR-0001).
  - **Фикс staticDir #20**: в standalone-сборке относительный `staticDir` (через `import.meta.url`) запекался в build-путь раннера (`/home/runner/.../public/media`) → на проде медиа 500 «missing on the disk». Решение: `Media.ts staticDir = process.env.MEDIA_DIR || <fallback>`; `MEDIA_DIR=/home/valstan/sabantuy/shared/media` добавлен в `/etc/sabantuy/sabantuy.env` (рантайм, переживает деплои). После редеплоя изображения (оригинал + thumbnail/card/wide) отдаются 200. (Кандидат в GOTCHAS — письмо brain.)
  - **Node-бамп**: `actions/setup-node@v4 → @v5` в `deploy-prod.yml` (Node 20 в actions депрекейтится 16.06.2026).
  - Прод-смоук вживую: `/`, `/gallery`, `/gallery/*`, `/o-sabantuy`, `/kontakty`, `/privacy` → 200; `/api/media/file/*` (orig + ресайзы) → 200 image/png.
- **Праздничный фолк-редизайн** (сессия 2026-06-05, PR #21, **на проде**): направление выбрано владельцем — «Фолк-праздник (максимал)».
  - **CSS-слой:** `web/src/app/(frontend)/tokens.css` (палитра изумруд/малина/золото/бордо/крем, fluid-типографика, ритм) + `patterns.css` (SVG-узоры тюльпан/ислими/4-лепестка/мазок как data-URI, `.eyebrow`/`.heading-brush`/`.frame-ornament`) + `festive.css` (секц-система `.section--tint/--alt/--green/--crimson`, герой, фича-карточки, кнопки `.btn-gold/-outline/-ghost`, адаптив 640/900/1200, `prefers-reduced-motion`, переоформление существующих классов). Все три `@import`'ятся из `globals.css` (порядок: tokens→patterns→festive).
  - **Шрифты:** `next/font/google` — Playfair Display (дисплей) + Manrope (текст), `subsets:['cyrillic','latin']`, переменные `--font-display/--font-body` на `<html>` в `layout.tsx`. Фолбэк через `var(--font-*, …)` (не дублировать в :root — конфликт специфичности с next/font).
  - **Компоненты** `(frontend)/components/`: Hero, SectionHeading, SectionDivider, FeatureCard/Row, GalleryPreview, CtaBand, MotifIcon (все серверные, inline-SVG). Конфиг фич — `lib/sabantuyFeatures.tsx`; excerpt из Lexical — `lib/lexicalExcerpt.ts`.
  - **Главная** (`page.tsx`): многосекционная витрина (герой с реальным фото → фича-ряд → тизер «О фестивале» → программа `ScheduleList` → превью галереи → тизер карты → CTA-полоса). 4 ISR-safe запроса через `Promise.all`, каждая секция за guard. Герой/лента берут «фото-альбом» = с **наибольшим числом снимков** (`sabantuy-2023`, 9 фото — живой репортаж, не афиша-постер), не newest-by-date.
  - **Логика не тронута:** `ScheduleList`/`RegistrationForm`/фильтры/live-next — только CSS-переоформление. `.is-live` остаётся зелёным.
  - Зелёным: tsc, eslint, `next build` (compile+типы+**prerender 10/10**; standalone EPERM — Windows-only G-known, CI/Linux ок). Прод-смоук: маршруты 200, фолк-разметка present, woff2 200. ⚠️ Локальный **preview-screenshot не снимает** эту страницу (тяжёлая + Next-overlay portal) — проверял через snapshot/inspect/network/eval; не баг страницы.
- **План улучшений сайта + первый шаг (сессия 2026-06-05, PR #27 + #28):**
  - **`docs/ideas/site-enhancements.md`** (PR #27) — живой бэклог из брейншторма (разведка реальных сайтов Сабантуев + наш стек + ограничения): разделы (Майдан-состязания, артисты, подворья, кухня, как добраться, FAQ…), интерактив (countdown, опросы, розыгрыши, «моя программа», регистрация на состязания, PWA), фото-оформление, мелочи. У каждого пункта ID/теги/оценка/чекбокс + сквозные правила (152-ФЗ #015, медиа/ADR-0001, миграции #017, ISR) + 3 волны. Разбираем по PR.
  - **V1+V3 фото-оформление главной** (PR #28, **на проде**): новый CSS-вариант `.section--photo` (реальный кадр праздника как фон секции + ислими/`hero-overlay`-вуаль + светлый текст с тенью) применён к секции «Что вас ждёт» и CTA-полосе; кадры — из «фоторепортажного» альбома (разные индексы, не дублируют обложку героя), нет фото → прежний цветной фолбэк. V3 — Ken-Burns зум (26s, scale 1→1.07) фону героя в `overflow:hidden`, `prefers-reduced-motion` гасит. Чистый фронт: без миграций/коллекций/PII.
  - Зелёным: lint, tsc, `next build` (prerender 10/10; EPERM standalone — Windows-only). **Прод-смоук вживую:** `/`→200, в разметке 2× `section--photo` с реальными `--section-photo:url(/api/media/file/seed-…jpg)`, оба фото-ассета→200 `image/jpeg`, CSS-бандл содержит `.section--photo` и `hero-kenburns`. ⚠️ Внешний прод-смоук — по **`https://1942c6fc87be.vps.myjino.ru`** (http даёт `UND_ERR_SOCKET`; G8-ротация — сверяться при обрыве).

## Прод-деплой (M3 ✅ — PR #10, сессия 2026-06-04)

**Работает публично:** `http://1942c6fc87be.vps.myjino.ru` и **`https://…`** (jino отдаёт 443 своим `*.vps.myjino.ru` сертификатом). `/` и `/admin` → 200. БД пустая → расписание показывает пустое состояние (контент/admin завести — см. «Следующий шаг»).

**Сервер** (myjino VPS, Ubuntu 24.04, **1 vCPU / 1.5 GiB / без swap**):
- Доступ: **`ssh sabantuy`** (в `~/.ssh/config`): host `1942c6fc87be.vps.myjino.ru`, user `valstan`, ключ `~/.ssh/id_ed25519_sabantuy` (изолированный #001), **sudo без пароля**. **Порты (сверено по панели 2026-06-04):** sshd VM = форвард **49348 → внутр. 22** (это и есть `valstan@`); **49338 = myjino-шлюз root** (не наш sshd). ⚠️ myjino ротирует host/port (G8).
- ⚠️ **SSH с локальной dev-машины к myjino может НЕ работать**, хотя VPS включён: edge myjino принимает TCP, но не отдаёт SSH-баннер на обоих портах (фильтр клиентского IP). При этом **раннер GitHub Actions достукивается** (другой IP). → DB-миграции и деплой гонять **через CI** (`apply-migration.yml` / `deploy-prod.yml`), не через локальный SSH. Это расширение G8.
- Стек: Node 22 (NodeSource) + pnpm 10 (corepack), PostgreSQL 17, nginx 1.30 (reverse-proxy `:80 → 127.0.0.1:3000`, ставит `X-Forwarded-For`/`X-Real-IP` для #005); certbot/ufw установлены, не настроены.
- БД: роль+БД `sabantuy` (`127.0.0.1:5432`), схема создана (21 таблица). Секреты — `/etc/sabantuy/sabantuy.env` (`root:valstan 0640`, systemd `EnvironmentFile=`): `DATABASE_URL`, `PAYLOAD_SECRET`, `NEXT_PUBLIC_SERVER_URL`, `ORGANIZER_EMAIL`, `NODE_ENV=production`, `PORT=3000`, `HOSTNAME=127.0.0.1`.
- Сервис: `sabantuy.service` (systemd, `node server.js` из standalone, `MemoryMax=1024M`), релизы `~/sabantuy/releases/<sha>`, симлинк `~/sabantuy/current`.

**CI-деплой** (`.github/workflows/deploy-prod.yml`): push в `main` (или `workflow_dispatch`) → сборка standalone на ubuntu (+ эфемерный postgres-сервис для prerender) → tar → `scp` на сервер → распаковка в релиз → `systemctl restart sabantuy` → smoke `/`. Секреты репо: `secrets.SSH_PRIVATE_KEY` (= приватный `id_ed25519_sabantuy`), `vars.DEPLOY_SSH_HOST/PORT`, `vars.NEXT_PUBLIC_SERVER_URL` — заданы. На сервере для `git pull` — read-only deploy key `~/.ssh/github_sabantuy` (добавлен в репо).

**Почему сборка в CI, а не на боксе:** `next build` на 1.5 GiB = OOM (пик 1.5G/1.5G), swap в контейнере myjino запрещён. Поэтому собираем в CI, на сервер кладём только готовый standalone (рантайм ≈150–300 МБ).

## Локальное окружение (per-machine — `web/.env` в `.gitignore`)

⚠️ Локальный Postgres **зависит от машины** — порт и роль НЕ универсальны. Не считай 5432/5433 константой проекта: сверяйся со своим `web/.env` (он gitignored, на каждой машине свой) и со своими службами Postgres. Известные сетапы:

| Машина | Postgres | Доступ в `web/.env` | Демо-контент |
|---|---|---|---|
| где разрабатывался M2 | порт **5433** (общий инстанс) | выделенная роль/БД `sabantuy` (полные права владельца); пароль суперюзера `postgres` — в `../GONBA/web/.env` | 4 события (2 с регистрацией), `privacy`, 1 заявка |
| сессия 2026-06-03 (рядом БД `matricarmz_dev`) | порт **5432** (служба `postgresql-x64-17`) | БД `sabantuy` под суперюзером `postgres/postgres` | 3 события (1 с регистрацией), `privacy`, 1 тест-заявка |

> На машине 2026-06-03 в `DATABASE_URL` обязателен **`127.0.0.1`**, не `localhost`: `::1:5432` даёт `Permission denied (10013)` (IPv6-loopback), IPv4 работает.

> Машина сессии 2026-06-04 (localization): локальная БД **пересоздана** под localization-схему (drop+push) и засеяна портером `seedFromWp.ts` — 3 страницы (`o-sabantuy`/`kontakty`/`privacy`), 2 альбома галереи (13 фото). Dev-админ пересоздан портером. Прежний демо-контент перезатёрт. Чтобы повторить на любой машине: `corepack pnpm -C web payload run src/seed/seedFromWp.ts` (idempotent).

Общее на всех машинах:
- **Dev-админ Payload:** `admin@sabantuy.local` / `SabantuyDev!2026` (роль admin) — заводится автоматически портером `seedFromWp.ts`, если в БД нет ни одного пользователя. Если на твоей машине пароль иной — выровняй на этот через `payload run` (`payload.update` user, `overrideAccess`).
- **Запуск:** `corepack pnpm -C web dev` → http://localhost:3000 · `/admin`.
- **M2 e2e зелёный** (проверено на обеих машинах): заявка `open+valid`→201 · закрытое событие→400 · без согласия→400 · неизв. `event`→400 · anon `GET /api/registrations`→403 (PII, #015) · `/privacy`→200 · email-хук в консоль.

## Следующий шаг (по приоритету)

> 🎯 **СЛЕДУЮЩАЯ СЕССИЯ — варианты.** (A) **Декор follow-up** (page-imagery): R1+R2+R3 на проде, директива закрыта. Опц. доводка — отрисовка отдельных сцен в `SceneIllustration.tsx` (плуг чуть абстрактный); ИЛИ если владелец даст per-topic реальные фото / появится image-gen — можно вернуть гибрид «фото-фон + иллюстрация-эмблема». CC-фото потребуют поля «кредит» в Media (мини-миграция #017). (B) **Татарский перевод** (договорено 2026-06-07, ниже).
>
> ✅ **Татарский перевод — добит (2026-06-07b, PR #74).** tt-**тела** `o-sabantuy`/`kontakty`/`privacy` переведены и залиты на прод сидом `seedTatar.ts` (раньше — только tt-заголовки, тела fallback на ru); + tt `title`+`description` альбомов галереи (`sabantuy-2023`, `afisha-programma-2024`). Прогон `seed-tatar.yml --ref main` зелёный + смоук; прод-смоук вживую: `/tt/o-sabantuy` (тело «бабайлар…»), `/tt/kontakty` («Идарә башлыгы»), `/tt/privacy` («Шәхси мәгълүматлар»), `/tt/gallery` (татарские названия альбомов, ru-варианта нет). Источник ru o-sabantuy/kontakty снят с **прод-API** (старый WP-домен теперь отдаёт наш сайт). В o-sabantuy убран устаревший «29 июня 2024». **Теперь весь tt-контент сайта татарский:** UI + 8 культ-разделов + 3 контент-страницы + галерея + intro карты + конфиги. **Осталось:** (1) ⚠️ **вычитка носителем** (весь tt — мой черновик); (2) опц. демо-Events (titles/summaries) — низкий приоритет, демо-данные заменит владелец. **Татарский перевод (договорено 2026-06-07): добито.** (1) Длинные RichText-**тела** WP-страниц `o-sabantuy`/`kontakty`/`privacy` (сейчас переведены только заголовки, тела — fallback ru) → дополнить `web/src/seed/seedTatar.ts` (есть Lexical-конструктор + паттерн) → прогон на проде `gh workflow run seed-tatar.yml --ref main`. (2) Контент **Events/Gallery** (демо-события titles/summaries, альбомы) — тем же сидом. (3) ⚠️ **Вычитка всего tt носителем** (мой перевод — черновик; правится в `seedTatar.ts`/`lib/i18n.ts` или прямо в `/admin` ru/tt-селектором). Механизм I11 + сиды готовы это принять — нужен только tt-текст. Источник ru-тел WP-страниц: на проде в `/admin` (или `seedFromWp.ts`).

1. **Домен + TLS** — ✅ **сделано владельцем** (2026-06-05). Если URL изменился — свериться, что `vars.NEXT_PUBLIC_SERVER_URL` указывает на боевой домен (значение бейкается в бандл при сборке) и nginx `server_name`/TLS под него.
2. **Первый admin на проде** — ✅ **сделан владельцем** (2026-06-05). Прод-БД наполнена: 3 страницы (`o-sabantuy`/`kontakty`/`privacy`), 2 альбома галереи (13 фото). Осталось контентом: добавить **события** (с площадкой/тайм-слотами) и заменить **шаблонный текст «Политики ПДн»** реквизитами оператора.
3. **⚠️ #017 миграции — ДО любого изменения схемы:** прод-рантайм **не** делает push (`push:true` гейтится `NODE_ENV=production` — см. «Не делать»). Первичная схема создана разовым dev-mode push. Любое изменение коллекций требует payload-миграций (`web/src/migrations/`) + применения на проде ДО деплоя (как у GONBA, ADR-0002/0003). Дать brain feedback (#017).
   - **Фреймворк миграций заведён** (этот PR, по образцу GONBA): `web/src/migrations/` (`.ts` + зеркальный `.sql` + `index.ts`), скрипты `migrate`/`migrate:create`/`migrate:status`, **CI migration-guard** в `deploy-prod.yml` (падает, если в коммите новые миграции; обход — `workflow_dispatch` после ручного применения, G6).
   - **Первая миграция `20260604_120000`** — добавляет `events.venue` (+ `_events_v.version_venue`, G7). Имена/типы взяты push-inspect'ом из dev-БД; идемпотентна (`ADD COLUMN IF NOT EXISTS`).
   - **`20260604_120000` УЖЕ применена на проде** (2026-06-04) через workflow `apply-migration.yml` (см. ниже про SSH). Проверено: `events.venue`=1, `_events_v.version_venue`=1, записано в `payload_migrations`. Деплой кода прошёл (dispatch, guard пропущен).
   - **Рабочий поток для будущих миграций (накат ДО мержа):** `gh workflow run apply-migration.yml --ref <feature-branch> -f migration=<ts>` (накат по SSH из CI) → merge PR → авто-деплой падает на migration-guard (прод цел) → `gh workflow run deploy-prod.yml --ref main` (деплой кода, guard пропускается на dispatch).
     - ⚠️ **`--ref <feature-branch>` обязателен**, если миграцию катишь ДО мержа: `workflow_dispatch` без `--ref` чекаутит дефолтную ветку (`main`), где файла `web/src/migrations/<ts>.sql` ещё нет → шаг падает «Нет файла …». На сессии 2026-06-05 первый dispatch (без ref) упал ровно так; повтор с `--ref feat/localization-tt-ru` прошёл. Preflight (read-only SSH) при этом отрабатывает в обоих случаях — обманчиво. (Кандидат в #017/GOTCHAS — письмо brain отправлено.)
4. **SMTP email-адаптер** — ✅ **код подключён и на проде** (PR #24, 2026-06-05). `@payloadcms/email-nodemailer`, провайдеро-независимо через env (`SMTP_HOST/PORT/USER/PASS/FROM_*`; любой relay — Resend/Brevo/SendGrid). Пока `SMTP_HOST` пуст — письма в консоль (поведение прежнее, сборка зелёная). **Чтобы письма пошли:** владелец (а) регистрирует relay → берёт SMTP-доступы; (б) дописывает `SMTP_*` + реальный `ORGANIZER_EMAIL` в `/etc/sabantuy/sabantuy.env`; (в) `sudo systemctl restart sabantuy`; (г) тест-заявка на сайте → письмо приходит, в `journalctl -u sabantuy` пропадает `No email adapter`.
5. **Season-MVP #2 «Карта фестиваля»** — ✅ **выкачена на прод (PR #15)**. Глобал `festival-map` + `/map`; миграция `20260604_140000` применена через `apply-migration.yml`, деплой зелёный. Осталось наполнение: владелец загружает **план территории** (картинка) в `/admin` → раздел «Карта фестиваля» + добавляет объекты. До загрузки `/map` показывает заглушку.
6. **Localization TT/RU** — ✅ **выкачена на прод** (PR #17, сессия 2026-06-05). (а) Миграция `20260604_160000` применена на проде через `apply-migration.yml` (накат ДО мержа, #017-поток); прод-смоук вживую: `/`, `/admin`, `/gallery`, `/map`, `/api/events` → 200 (схема/код синхронны). Осталось: (б) **публичный язык-тумблер** — позже, когда появятся реальные TT-переводы (cookie/locale + locale-aware `payload.find`, осторожно с ISR); (в) открытый вопрос владельцу — кто переводит TT-контент.
7. Опц.: deprecation `actions/setup-node` → ✅ закрыто, везде `@v5` (deploy-prod; `apply-migration`/`seed-prod` ставят Node на боксе по SSH — `setup-node` не используют); фронт (hero-изображения событий через админку; фильтр по дням ✅ сделан); rate-limit при масштабе → Redis. **Deploy-smoke #011** — ✅ **сделано** (PR #23): шаг «Smoke check — local routes» бьёт `/`+`/map`+`/admin` одним ssh-заходом, собирает все провалы. На проде зелёный: /→200, /map→200, /admin→200.

## Открытые вопросы / решения для владельца

- Кто будет редактором-организатором (роль `editor`).
- Внешнее хранилище медиа (ADR-0001): подтвердить Я.Диск или иной провайдер.
- Punycode домена `сабантуймалмыж.рф` — при настройке DNS/nginx.
- Текст «Политики обработки ПДн» сейчас **шаблонный** (плейсхолдеры реквизитов оператора) — владелец должен заполнить реальными данными организатора.
- **Тайм-слоты программы** (время/площадка событий) — орги вводят в `/admin` **до** мероприятия. Фича готова, контент — за владельцем.
- **Asset плана территории** для «Карты фестиваля» (season-MVP #2) — нужен от оргов.
- **Татарская версия (localization)** — подтвердить, что нужна, и кто переводит TT-контент (вручную). Архитектурно включается на M3.

## Не делать / тупиковые подходы

- `create-payload-app` в Claude Code не работает (TTY/clack, G10). Каркас — вручную по образцу GONBA.
- pnpm 11 несовместим — только pnpm 10 через corepack.
- **Next 15 metadata-файлы (`robots.ts`, `sitemap.ts`) держать в корне `app/`, НЕ в route-group `(frontend)`** — иначе `robots.txt` молча не генерится (sitemap «повезло», robots — нет). Грабля отправлена brain (PR #6).
- **`curl` в git-bash на Windows искажает кириллицу в теле запроса** (cp1251 вместо UTF-8) → авто-slug приходит пустым, заголовки — мусором. Для API-тестов с не-ASCII использовать **node fetch** (UTF-8), НЕ curl. Само приложение/`slugField` корректны (проверено). Плюс git-bash съедает ведущий `/` в `curl -w "%{...}"` (MSYS path-conversion) — коды верные, текст шаблона мусорный.
- **myjino VPS — контейнер, swap запрещён** (`swapon: Operation not permitted`). На 1.5 GiB `next build` (Payload) ловит OOM (пик 1.5G/1.5G). Не пытаться собирать на боксе и не лить swapfile — собирать в CI, на сервер только standalone-артефакт.
- **Payload `push:true` гейтится `NODE_ENV`** — в production push НЕ выполняется. Прод-рантайм схему не создаёт/не меняет. Первичную схему накатывать разовым `payload run` с `NODE_ENV=development`; дальнейшие изменения — миграции (#017). НЕ полагаться на рантайм-push в проде.
- **`drizzle-kit push` уходит в ИНТЕРАКТИВ на структурных правках** (напр. включение localization: колонки переезжают в `*_locales`). На непустой БД спрашивает «`snapshot` — новая колонка или rename `version_title`?» и **виснет на stdin** (в Claude Code / headless — мёртвый замок). Обход для генерации DDL: `DROP SCHEMA public CASCADE; CREATE SCHEMA public` → push на **пустую** БД создаёт схему без интерактива → `pg_dump -t <table>` снимает точный DDL для миграции. Верификация миграции: `git stash` → push пустую = baseline → применить миграцию `psql`'ом → `pg_dump` → дифф против каноничного push (письмо brain 2026-06-04, кандидат в #017/GOTCHAS).
- **Payload `upload.staticDir` через относительный путь (`import.meta.url`) ломается в standalone-сборке Next** — путь «запекается» в АБСОЛЮТНЫЙ путь сборочной машины (`/home/runner/.../web/public/media`), на проде файлы не находятся (медиа 500 «missing on the disk», страницы при этом 200). Решение: `staticDir = process.env.MEDIA_DIR || <fallback>`, на проде `MEDIA_DIR` в env (рантайм). Каталог медиа держать ВНЕ релиз-директории (симлинк в `shared/`), иначе деплой стирает загрузки. (Сессия 2026-06-05, PR #20.)
- **nginx на сервере — из репо `nginx.org` (mainline 1.30)**, его apt-репо отключён (`/etc/apt/sources.list.d/nginx.list.disabled`) из-за NO_PUBKEY — иначе падает `apt update` и за ним NodeSource-setup. Нужны апдейты nginx — добавить ключ и вернуть `.list`.
