# План улучшений сайта «Сабантуй Малмыж» — функционал и оформление

> Живой бэклог идей для развития сайта: новые разделы, интерактив, фото-оформление.
> Собран из (1) разведки реальных сайтов Сабантуев + фестивальных best-practices,
> (2) того, что у нас **уже есть**, (3) наших проектных ограничений.
> Разбираем по одному PR за раз; статус отмечаем чекбоксом и номером PR.
>
> Создан: 2026-06-05. Источник идей — сессионный брейншторм (см. «Источники» внизу).

**Легенда тегов:** ⚙️ чистый код · ✍️ нужен контент/фото от владельца · 🔒 персональные данные (152-ФЗ) · 🎨 визуал · ⏳ «событийность».
**Трудоёмкость:** **S** (часы) · **M** (день-два) · **L** (несколько дней).
**Статус:** ☐ идея · ◐ в работе · ✅ на проде.

---

## Что уже есть (база, на которую опираемся)

- **Главная-«ворота»** (фолк-редизайн, PR #21): Hero с реальным фото, фича-ряд «Что вас ждёт», тизер «О фестивале», программа (`ScheduleList`), превью галереи, тизер карты, CTA-полоса.
- **Маршруты:** `/` · `/events/[slug]` (детали + форма заявки) · `/map` · `/gallery` + `/gallery/[slug]` · `/o-sabantuy` · `/kontakty` · `/privacy` · `/[slug]`.
- **Коллекции/глобалы:** `Events` (с `venue`), `Gallery`, `Media` (size-варианты thumbnail/card/wide), `Pages`, `Registrations` (152-ФЗ), `Users`; глобал `festival-map`.
- **Функции:** публичная регистрация с rate-limit + серверным гейтом, ISR `revalidate=60`, sitemap/robots, кастомная 404, **SMTP-адаптер готов** (PR #24), двуязычие RU/TT (в админке; публичный тумблер отложён).
- **Стиль:** `tokens.css` (палитра изумруд/малина/золото/бордо/крем) + `patterns.css` (SVG-узоры) + `festive.css`; шрифты Playfair Display + Manrope; компоненты `Hero/SectionHeading/SectionDivider/FeatureCard/Row/GalleryPreview/CtaBand/MotifIcon`.

---

## Сквозные правила (соблюдать в каждом PR из этого плана)

- **🔒 152-ФЗ.** Любая новая форма с контактами (розыгрыш, регистрация на состязания, подписка) → галка согласия + ссылка на «Политику ПДн» + **серверный authz #015** (read закрыт на персонал) + rate-limit. Анонимные опросы и «Моя программа» PII не собирают → проще.
- **📦 Медиа/трафик.** Больше фото = больше трафика на 1.5 ГБ VPS в пик. Лечится size-вариантами `Media` + lazy-load; тяжёлое (большие галереи, UGC) → **внешнее хранилище** ([ADR-0001](../adr/0001-media-external-storage.md)).
- **🧱 Схема.** Новые коллекции/поля → миграции **#017** (push-inspect + накат ДО мержа из CI, см. handoff §«#017»).
- **⚡ ISR vs live.** Countdown / опросы / «сейчас на майдане» — клиентские компоненты или короткий `revalidate`, не ломая ISR-60.
- **🎨 Единый стиль.** Переиспользовать существующие токены/узоры/компоненты, не плодить новый визуальный язык.
- **PR-flow.** Ветка → гейты (`typecheck`/`lint`/`build`) → PR → зелёный CI → авто-мерж (ADR-0002 + автономия под гейтами #027; см. CLAUDE.md §PR-only flow).

---

## A. Оживить оформление настоящими фото

Сейчас декор есть (палитра + SVG-узоры), но «фотографичности» мало — живое фото только в герое.

- ✅ **V1 — Фотофоны секций.** Вариант `.section--photo`: реальный кадр + полупрозрачная цветная вуаль поверх (читаемость текста). Чередовать с однотонными. 🎨⚙️ **S** — *на проде (PR #28): «Что вас ждёт» + CTA на реальных кадрах; прод-смоук — 2 фото-секции, ассеты 200 image/jpeg*
- ✅ **V2 — Фотолента-разделитель.** Полоса из 6 реальных кадров праздника между секциями главной (`PhotoStripDivider`, между хабом и программой; мобайл — 3 кадра). 🎨⚙️ **S** — *этот PR; нет фото → не рендерится.*
- ✅ **V3 — «Живой» герой.** Медленный зум (Ken Burns) + градиент-вуаль изумруд→прозрачность + узор по кромке; всё с `prefers-reduced-motion`. 🎨⚙️ **S** — *на проде (PR #28): Ken-Burns зум фону героя; CSS-бандл содержит `hero-kenburns`*
- ✅ **V4 — Мозаика-фотостена + лайтбокс.** Masonry-сетка (CSS-колонки, фото в натуральных пропорциях) в альбоме `/gallery/[slug]` + клиентский **лайтбокс** (`AlbumGallery.tsx`: клик по фото → оверлей с ←/→, Esc, кликом по фону, блокировкой скролла). 🎨 **M** — *этот PR. Превью локально — dev-сид `seedDemoGallery.ts` (плейсхолдеры разной высоты; WP-источник реальных фото пропал). Бонус: лайтбокс заменил «открыть сырой файл в новой вкладке».*
- ✅ **V5 — Фото в рамке.** Плитки masonry — в золотой рамке + тень/hover-подъём (альбом). Орнамент-рамка `.frame-ornament` (золотая двойная + уголки) применена к ключевым кадрам главной — «О фестивале» и тизер-фото. 🎨⚙️ **S** — *сделано в редизайне (PR #21, `page.tsx` figure.frame-ornament); пометка обновлена.*
- ✅ **V6 — Декор прозовых страниц «ни одной сухой страницы»** (директива brain `page-imagery`). Контент-aware шапка всех `Pages`-by-slug: орнамент-слой (мотив-медальон + текстура + eyebrow + vine-divider) → `lib/pageDecor.ts` (slug→{сцена,иконка,акцент,eyebrow}, ru/tt). 🎨⚙️ **M** — *R1 на проде (PR #68). R2 (реальные фото в шапках, PR #70) заменён R3.*
- ✅ **V7 — Тематические SVG-иллюстрации по смыслу.** 11 фолк-сцен, нарисованных кодом (`SceneIllustration.tsx`: көрәш, казан+чак-чак, хоровод народов, нац. дом, плуг+колос, змей+мяч, дорога, FAQ-пузыри, конверт+метка, документ+щит, солнце+тюльпаны) — крупно в шапке вместо медальона. Тир-2 политики (своя графика без копирайта; AI-генерация недоступна в сессии). 🎨⚙️ **M** — *на проде (PR #72). Verify «вслепую» через sharp→PNG→Read (скриншот превью не работает). Опц. доводка отдельных сцен (плуг абстрактный).*

---

## B. Новые разделы-контент (взято у настоящих Сабантуев)

> **Волна 1 разделов выкачена контент-страницами** (коллекция `Pages`, **без схемных
> миграций** — только данные) сидом `web/src/seed/seedCulture.ts`. На главной —
> секция-хаб **«Традиции и культура»** (зелёная, 6 карточек-мотивов) + ссылки в
> подвале сайта. На прод заливаются workflow `seed-culture.yml` (после деплоя кода).
> Тексты — «рыба» хорошего качества из открытых источников (переписи, краеведение);
> даты/цены/адреса организаторы правят в `/admin`. Структурные апгрейды (отдельные
> коллекции `Competitions`/`Artists`/`Sponsors` с фото-карточками) — по мере надобности.

- ◐ **R0 — 🤝 Народы Малмыжского края.** ⭐ Многонациональность района: **марийцы** (древнейшие жители, дали имя городу — «Малымаш» = ночлег), **татары** (≈треть населения, мечети/медресе, сёла Старый Ирюк, Татверхгоньба…), **русские** (крепость 1584), **удмурты**; Сабантуй как праздник дружбы народов. Страница `/narody`. ✍️ **M** — *контент-страница готова; на прод — `seed-culture.yml`. Флагман запроса «местный колорит + многонациональность».*
- ◐ **R1 — 🏆 Майдан: состязания и традиции.** Ядро праздника: **куреш** (борьба на поясах → батыр + баран), **скачки**, **столб с призом**, **бой мешками на бревне**, разбивание горшка, бег с яйцом / в мешках, перетягивание каната, монета в катыке, поднятие гири. ✍️ **M** — *контент-страница `/maydan` готова (тюльпан-списки забав). Апгрейд до коллекции `Competitions` с фото-карточками — позже.*
- ☐ **R2 — 🎤 Артисты и сцена.** Хедлайнеры + расписание сцены. Коллекция `Artists` (имя, фото, время, сцена, соцсети); блок «Хедлайнеры» на главной. ✍️⚙️ **M**
- ◐ **R3 — 🏘️ Подворья и ярмарка.** Районные подворья, ремесленники, маркет нац. одежды. ✍️ **M** — *контент-страница `/podvorya` готова (татарское/русское/марийское/удмуртское подворья + ярмарка ремёсел — на тему многонациональности). Фото-карточки/коллекция — позже.*
- ◐ **R4 — 🍲 Кухня Сабантуя.** Чак-чак, эчпочмак, баурсак, кош теле, катык, губадия; плов из казана. ✍️ **S-M** — *контент-страница `/kuhnya` готова (татарские блюда + общий стол народов края). Фото-секция «что попробовать и где» — позже.*
- ◐ **R5 — 🧒 Детский майдан.** Детские игры, мастер-классы, шоу. ✍️ **S** — *контент-страница `/detskiy-maydan` готова (игры, мастер-классы, сладости).*
- ◐ **R6 — 🚌 Как добраться.** Дорога на машине/автобусом, парковка, отсылка к «Карте». ✍️⚙️ **M** — *контент-страница `/kak-dobratsya` готова (география Малмыжа на Вятке; точные адреса/расписания — за организаторами).*
- ☐ **R7 — 🤝 Партнёры и спонсоры.** Логотипы со ссылками. Глобал `sponsors` (логотип+ссылка+уровень). ✍️⚙️ **S**
- ◐ **R8 — ❓ FAQ.** Что взять, можно ли с детьми, дождь, парковка, участие, еда. ✍️⚙️ **S** — *контент-страница `/faq` готова (6 вопросов-ответов). Апгрейд до аккордеона/коллекции — позже.*
- ◐ **R9 — 📜 История Сабантуя.** Этимология «сабан»+«туй», от деревенского обряда к народному торжеству, в Малмыже. ✍️ **M** — *контент-страница `/istoriya-sabantuya` готова. Таймлайн по годам с фото (`sabantuy-2023`, `afisha-2024`) — позже.*

---

## C. Интерактив и вовлечение

- ✅ **I1 — ⏳ Обратный отсчёт.** Клиентский `Countdown.tsx` (тик 1с, hydration-safe, не ломает ISR); цель — ближайшее будущее событие (без отд. глобала/миграции). 🎁 **Демо-программа** (`seedDemoProgram.ts`, 12 событий-микс по прошлым малмыжским Сабантуям, slug `demo-*`, ориентир-дата 27.06.2026) + workflow `seed-program.yml`. ⚠️ **Обязательный видимый дисклеймер** (`FestivalNotice`, флаг `lib/festival.ts → DEMO_SCHEDULE`): «дата предварительная, не настоящая; точные дату/место организаторы объявят позже» — у отсчёта, в программе и на странице события. *Когда будут реальные данные: `DEMO_SCHEDULE=false` + удалить `demo-*` события.* ⚙️⏳ **S**
- ✅ **I2 — ⭐ «Моя программа».** Звёздочка ☆/★ у события → личный список в `localStorage` (`sabantuy:my-program`), **без БД/PII**, ISR-safe; чип-фильтр «★ Моя программа (N)» в `ScheduleList`. ⚙️ **S-M** — *этот PR; чисто фронт, hydration-safe (грузим звёзды после монтирования).*
- ✅ **I3 — 📊 Опрос «Любимое состязание».** Коллекция `poll-votes` (#015: create=anyone, read закрыт; итоги — серверный агрегат `count`, overrideAccess); голос → `POST /api/poll-votes` + оптимистичные бары; «один голос на браузер» (localStorage) + rate-limit по IP (`rateLimitPoll`); невалидный вариант → 400 (enum). ⚙️🔒 **M** — *этот PR. **Миграция `20260606_164700`** (#017): новая таблица `poll_votes` + enum + связка `payload_locked_documents_rels.poll_votes_id`/FK/idx (полный push-inspect — дамп одной таблицы упускал rels-связку, см. письмо brain). Верифицирована: drop→apply→5 объектов→идемпотентно.*
- ✅ **I4 — 🎁 Розыгрыш призов.** Коллекции `Raffle` (title/prize/isOpen/drawNow/winner/drawnAt; read=anyone, winner field-locked #015) + `RaffleEntry` (ФИО/телефон/consent/raffle, ПДн, read=персонал). Рандом-победитель — галка `drawNow` в админке → хук `drawRaffleWinner` (серверный Math.random, выбор + закрытие приёма + сброс галки). Форма на главной (секция, только при открытом розыгрыше) + `rateLimitRaffleEntry` + серверный gate `enforceRaffleOpen`. **Миграция `20260607_020000`** (#017): таблицы `raffle`+`raffle_entries` с перекрёстными FK (winner↔raffle, ON DELETE SET NULL) + enum + rels; верифицирована drop→apply→pg_dump-дифф **1:1 с push**. Поля не локализованы (день-в-день фича). 🔒⚙️ **M-L** — *этот PR. Сам розыгрыш орги проводят галкой в админке в день праздника.*
- ✅ **I5 — 🎟️ Регистрация на состязания.** Поле `competitionType` (select-дисциплина: куреш/скачки/бой мешками/бег в мешках/канат/гиря/столб/детские) в `Registrations`; список — `lib/competitions.ts`. Селектор на форме показывается, когда событие — состязание (`category` sport/kids); заголовок секции → «Заявка на состязание». Тот же 152-ФЗ-гейт (consent/rate-limit/#015 read=персонал) переиспользован без изменений; значение валидируется против enum (Payload). **Миграция `20260606_180000`** (#017): enum `enum_registrations_competition_type` + nullable колонка `registrations.competition_type` (коллекция не версионируется → одна таблица, без `_v`/G7). Аддитивно (CREATE TYPE + ADD COLUMN). 🔒⚙️ **M** — *этот PR. Миграция применена+проверена на dev-БД (колонка+enum+8 значений, drizzle-конвенция как poll_votes). На прод — `apply-migration --ref` ДО мержа (#017/G28).*
- ✅ **I6 — ✉️ Подписка на анонс.** Коллекция `Subscribers` (email `unique`+дедуп, name, consent 152-ФЗ, source-locked; #015 read=персонал) + форма «Напомнить о празднике» на главной (перед CTA) + rate-limit (`rateLimitSubscribe`) + письмо-подтверждение подписчику (`confirmSubscription`, graceful через SMTP #24). **Миграция `20260607_010000`** (#017): таблица `subscribers` + enum `enum_subscribers_source` + UNIQUE-индекс email + rels-связка; верифицирована drop→apply→pg_dump-дифф **1:1 с push**. 🔒⚙️ **S-M** — *этот PR. Сама рассылка-напоминание — позже, орги вручную ближе к дате.*
- ☐ **I7 — 🗺️ Интерактивная карта.** Фильтр пинов по типам (показать/скрыть «еда/сцена/туалеты»), опц. «вы здесь». Расширение `/map`. ⚙️ **M**
- ◐ **I8 — 🖼️ «Народная лента» гостей (UGC фото/видео).** _В работе с 2026-06-27 (раньше «после сезона» — владелец решил к фестивалю)._ Посетители снимают на телефон → выкладывают фото/короткие видео прямо на сайте (подготовка + сам праздник), лайки/комменты/шеринг (типа TikTok/VK). Решения: полная лента, **постмодерация + «пожаловаться»**, прямая загрузка, **Yandex Object Storage (S3)** (медиа минует наш слабый бокс — браузер↔S3). **PR1/6 на проде** (#147: S3-обёртка + presigned-endpoint). План: [`.claude/plans/sabantuy-ugc-feed.md`](../../.claude/plans/sabantuy-ugc-feed.md). Блокер: владелец настраивает Object Storage ([`docs/ops/yandex-object-storage-setup.md`](../ops/yandex-object-storage-setup.md)). 🔒⚙️ **L**
- ✅ **I9 — 🔗 Шаринг + OG-картинки.** Брендовый OG/Twitter-баннер `/og.jpg` (1200×630, генератор `src/seed/genOgImage.ts` — sharp SVG→jpg, кириллица ок) + `openGraph`/`twitter`/`metadataBase` в `layout.tsx`. 🎨⚙️ **S-M** — *этот PR; статичный баннер (надёжнее динамического OG-image с кириллицей в standalone). Динамические per-event превью — позже при желании.*
- ✅ **I10 — 📱 PWA + офлайн-программа.** Web App Manifest (`app/manifest.ts` → `/manifest.webmanifest`, в корне `app/` — G12) + брендовые иконки (192/512/maskable/apple, генератор `seed/genPwaIcons.ts`, тюльпан на изумруде) + рукописный service worker (`public/sw.js`, без зависимостей): навигации **network-first** (ISR честен онлайн, кэш/`/offline` без сети), статика/медиа **cache-first**, `/admin`+`/api` мимо кэша. Регистрация (`ServiceWorkerRegister.tsx`) только в production. `theme-color`/`appleWebApp` в layout. ⚙️ **M** — *этот PR. Верифицировано: build (manifest+offline ○ static), HTTP 200 на всех ассетах, `<head>`-теги впрыснуты, `node --check` SW. Полный офлайн-цикл — в реальном браузере (headless-превью SW не гоняет).*
- ✅ **I11 — 🌐 Тумблер RU/TT.** Путевая схема (ru на `/`, tt на `/tt`): `lib/i18n.ts` (словарь UI ~90 строк) + `localeHref` + `SiteChrome` (клиентский, локаль из usePathname → ISR цел) + `_views/*` (общие тела) + `/tt/*` обёртки + payload.find({locale}) с fallback ru + sitemap hreflang. Перевод контента — `seedTatar.ts` (8 культ-разделов title+body + карта-intro в tt) + `seed-tatar.yml`. ⚙️ **M** — *механизм + контент-черновик на проде. ⚠️ Татарский — мой ЧЕРНОВИК, нужна вычитка носителем (правится в `lib/i18n.ts` и `/admin`). Длинные тела o-sabantuy/kontakty + lib-конфиги (категории/карта-метки/фича-ряд) пока ru — доводка.*
- ✅ **I13 — 🎯 Познавательная игра-угадайка.** Викторина про Малмыжский край + Сабантуй (директива brain 2026-06-18). Коллекция `quiz-questions` (versioned+drafts, read=published, write=adminOrEditor #015): `prompt`/`options[text(localized)+correct]`/`explanation`/`hint` RU/TT, `source` (фактчек), `theme`/`format`/`difficulty`, `key`. `_views/QuizView` + `/igra`+`/tt/igra` + клиентский `QuizGame` (механика, прогресс в localStorage, Web Share, звания, shuffle-on-click). **Миграция `20260618_140000`** (8 таблиц versioned+localized-array + 9 enum'ов, push-inspect, verify down→up→pg_dump 1:1). **Сид `seedQuiz.ts`** — 16 вопросов RU+TT, каждый с проверяемым `source` (ru.wikipedia/culture.ru/tatarica). `seed-quiz.yml`. ⚙️🎨 **L** — *на проде (PR #106). **Расширено 2026-06-24/25:** `/igra` — хаб двух игр (PR #124/#125); **«Знаток Сабантуя» 16→33** (+17 с adversarial-фактчеком, тема `language`; колода = 12 случайных, `QUIZ_DECK_SIZE`; PR #132) + **«Угадай по картинке» 8→10** на свободных Commons-фото, чистка неудачного калфака + тюбетейка/гармонь (лицензии по Commons API, визуально проверены; PR #134). tt — черновик (вычитка в PENDING). (I12 зарезервирован под per-event OG — см. PENDING.)*
- ✅ **I14 — 📊 Счётчик игроков + статистика игры.** На `/igra` витрина: всего игроков, средний балл, распределение «N посетителей — X из 16» (RU/TT, ru-плюрализация), свой результат подсвечен. Коллекция `quiz-results` (`score`/`total`, без PII; #015: create=anyone, read/update/delete=adminOrEditor → агрегат сервером, `GET /api/quiz-results`→403). Хук rate-limit + валидация целостности (подделка score>total→400). Сервер count-бакетами; клиент шлёт 1 POST на браузер (localStorage) + оптимистичный bump. **Миграция `20260618_160000`** (таблица `quiz_results`, push-inspect, verify 1:1). ⚙️ **M** — *на проде (PR #109): `/igra`+`/tt/igra` витрина с пустым состоянием («будьте первым»), read→403, смоук зелёный.*
- ✅ **I16 — 🖼️ Лайтбокс «Народной ленты».** *Готово 2026-06-28 (PR #174, на проде):* `LentaLightbox` — клик по фото/видео в `/lenta` → полноэкранный оверлей с пролистыванием всех медиа (× / клик по фону / Esc, кнопки ‹/›, клавиши ←/→ на десктопе, свайп пальцем на телефоне, видео в оверлее, a11y focus-trap + scroll-lock, wrap-around). ISR ленты не тронут (○ Static). Образец — `AlbumGallery.tsx` + touch-свайп + видео.
- ✅ **I15 — 📈 Счётчики посещаемости + SEO/GEO.** Яндекс.Метрика 109964170 (невидимая, Вебвизор/ssr) + видимый бейдж LiveInternet `t29.6` в подвале — env-gated (`NEXT_PUBLIC_YANDEX_METRICA_ID`/`NEXT_PUBLIC_LIVEINTERNET`, vars репо), отложенная загрузка, плашка-согласие 152-ФЗ. SEO/GEO (всё серверное, 0 JS): Schema.org JSON-LD (Event/FAQPage/Org/WebSite/Breadcrumb, RU/TT), `/llms.txt` (карта для ИИ-краулеров), robots под ИИ-ботов (GPTBot/ClaudeBot/PerplexityBot/Google-Extended/YandexBot…) + Host, canonical+hreflang. Без миграций. ⚙️🎨 **M-L** — *на проде (PR #111/#113/#115): оба счётчика активны, JSON-LD/llms.txt/robots проверены на проде. Осталось: клауза аналитики на живой `/privacy` (owner-gated, PENDING).*

---

## D. Мелочи праздничности

- ✅ **D1 — Лепестки** над героем. Тюльпан-лепестки (золото/малина), медленный дрейф со сносом+вращением, низкая прозрачность; чистый CSS-декор (`Petals.tsx` — серверный, без JS/гидрации; слой `aria-hidden`/`pointer-events:none` над вуалью, под текстом). Полностью гаснет под `prefers-reduced-motion` (`display:none`). ⚙️ **S** — *этот PR. Конфетти-вариант отклонён в пользу спокойного дрейфа (в духе «фолк», не «вечеринка»). Анимация — в реальном браузере; в сборке проверены keyframes+слой.*
- ☐ **D2 — Блок «Сейчас на майдане»** с фото площадки (live/next-бейджи уже есть). ⚙️ **S**
- ☐ **D3 — Бейджи «Новинка-2026»** на новых событиях. ⚙️ **S**
- ☒ **D4 — Фоновая музыка/звук — НЕ делать** (навязчиво). *(решено: отклонено)*

---

## Приоритизация по волнам

**Волна 1 — быстро, эффектно, без/мало PII и контента (можно начинать сразу):**
`I1` Countdown · `V1` Фотофоны · `V2` Фотолента · `R1` Майдан-состязания · `I2` Моя программа · `I3` один анонимный опрос.
→ *рекомендованный старт: `I1`+`V1` (чистый код, виден результат сразу, не зависит от контента владельца).*

**Волна 2 — нужен контент/фото от владельца:**
`R2` Артисты · `R3` Подворья · `R4` Кухня · `R5` Детский майдан · `R6` Как добраться · `R7` Партнёры · `R8` FAQ · `R9` История · `V3`/`V4`/`V5` визуал.

**Волна 3 — PII / тяжёлое, по сезону:**
`I4` Розыгрыш · `I5` Регистрация на состязания · `I6` Подписка · `I8` UGC-фотостена (#4) · `I9` OG · `I10` PWA · `I11` RU/TT-тумблер.

---

## Источники разведки

- [10 главных игр Сабантуя — АиФ Казань](https://kazan.aif.ru/society/details/vnov_pobyt_rebenkom_10_glavnyh_igr_sabantuya)
- [Программа Сабантуя в Казани 2025 — KP.RU](https://www.kazan.kp.ru/daily/27714/5102567/)
- [Традиции и состязания — life.ru](https://life.ru/p/1753903)
- [Креативный Сабантуй 2025 — allfest.ru](https://allfest.ru/festival-2025/kreativnyy-sabantuy)
- [Festival app features — Eventbase](https://www.eventbase.com/festival-app)
- [Афиша/площадки — culture.ru](https://www.culture.ru/events/5839842/sabantui-2025-nacionalnyi-prazdnik)
