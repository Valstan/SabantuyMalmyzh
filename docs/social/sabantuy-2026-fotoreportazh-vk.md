# VK-пост «Сабантуй-2026: большой фоторепортаж» — материалы и статус публикации

Готовый пост для рассылки по сети SARAFAN (сообщества «… - ИНФО» Кировской области и
Татарстана). Владелец одобрил текст и коллаж 2026-07-11. **Публикация приостановлена** —
нет живого админ-аккаунта (см. блокеры ниже). Пост «сохранён» для продолжения.

## Ассеты
- **Коллаж:** [`sabantuy-2026-fotoreportazh-vk.jpg`](sabantuy-2026-fotoreportazh-vk.jpg) — вертикаль 1080×1350, 6 кадров гранёной мозаикой + надпись в 3 строки. Генератор: [`web/scripts/make-vk-collage.mjs`](../../web/scripts/make-vk-collage.mjs) (source-фото — `web/src/seed/assets/vk-obzor2-2026/`).
- **Пост на сайте (источник):** `/novosti/sabantuy-2026-fotoreportazh` (на проде).

## Текст поста (одобрен)
```
🌾 Сабантуй-2026 в Малмыже — спасибо за праздник!

Около 20 тысяч гостей из четырёх регионов, национальные подворья, конные скачки, борьба корэш, гармонь и танцы до вечера. Мы собрали большой фоторепортаж этого дня.

📸 Все фото и видео праздника:
сабантуй.вмалмыже.рф/novosti/sabantuy-2026-fotoreportazh

📷 Поделитесь и своими кадрами в «Народной ленте» — выкладывайте фото и видео Сабантуя, их увидят все:
сабантуй.вмалмыже.рф/lenta

#Сабантуй #Малмыж #Сабантуй2026
```

## Цели — 16 групп сети (в БД SARAFAN: `regions`)
- **Киров (13):** Кировская область (-168170001), Арбаж (-221504685), Верхошижемье (-221515888), Вятские Поляны (-166980909), Кильмезь (-168172770), Лебяжье (-170437443), **Малмыж (-158787639)**, Нема (-168169352), Нолинск (-179306667), Пижанка (-221492354), Советск (-221480320), Тужа (-239050321), Уржум (-168170215)
- **Татарстан (3):** Татарстан (-239149826), Балтаси (-179203620), Кукмор (-180812597)

## Статус
- ✅ Тест-пост выложен в **Малмыж-ИНФО** через VITA → ушёл в статус **`suggest`** (предложенная новость), пост-id `-158787639_77214`. Публично «запись не найдена» (предложенные не видны). **Оставлен в очереди** — ждёт ручного одобрения.

## Блокеры публикации (проверено эмпирически 2026-07-11)
| Токен | wall.post / фото | Диагноз |
|---|---|---|
| COMM_* (community-токены всех 16 групп) | ❌ | `[214] method is unavailable with group auth` (постить нельзя) + `[27]` на загрузку фото |
| VALSTAN (единственный админ-аккаунт) | ❌ | `[5] user is blocked` — VK заблокировал аккаунт |
| VITA | ⚠️ только «предложить» | загрузка фото и `wall.post` проходят, но пост падает в `suggest` (VITA не редактор с правом публикации) |

**Вывод:** ВК не даёт публиковать сообщество-токенами (ограничение самого ВК). Нужен
живой user-токен админа с правом публикации. Пока такого нет.

## Как продолжить (когда появится путь)
1. **VALSTAN разблокируют** → публикация всех 16 автоматом. Скрипт: user-токен админа →
   на каждую группу `photos.getWallUploadServer`+`saveWallPhoto` (фото владелец=группа) →
   `wall.post(owner_id=-gid, from_group=1, message, attachments=photo…)`. Переиспользовать
   `modules/publisher/vk_wall_photo_upload.upload_wall_photo` из SARAFAN (на боксе setka,
   venv `/home/valstan/SETKA/venv`). Темп ≤ 1 пост / 5 c (probe: 16@5c без капчи).
2. **VITA-suggest → ручное одобрение владельцем** (если у него есть админ-доступ к группам
   через личный ВК): предложить пост во все 16 через VITA, владелец одобряет в каждой
   (Сообщество → Предложенные новости → Опубликовать). Малмыж уже предложен (77214).
3. **Новый админ-токен / повысить VITA до редактора с публикацией** → как п.1.

Токены и БД сети — на боксе `setka` (`ssh setka`, `/etc/setka/setka.env` root-only → через
`sudo`; таблицы `vk_tokens`/`communities`/`regions`). Значения токенов бокс не покидают.
