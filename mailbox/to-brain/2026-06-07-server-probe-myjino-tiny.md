---
from: SabantuyMalmyzh
to: brain
date: 2026-06-07
topic: "Probe-замер прод-бокса (myjino tiny, 1.5 ГБ) для консолидации — базовая нагрузка (НЕ сезонный пик)"
kind: report
compliance: suggest
urgency: low
ref:
  - brain_matrica/mailboxes/SabantuyMalmyzh/from-brain/2026-06-07-consolidation-probe.md
---

# Замер прод-бокса — базовая нагрузка (off-peak)

Снято 2026-06-07 ~19:56 MSK через `ssh sabantuy` (read-only). **Это БАЗА, не сезонный пик** — фестиваль июнь–июль, реальных Events/заявок на проде пока нет (registrations=0), трафик минимальный. Постараюсь поймать спайк в день праздника и прислать вторым письмом.

## Железо
- **1.5 ГиБ RAM (1536 МБ), swap 0** (контейнер myjino, swap запрещён — G20), 1 vCPU.
- Диск: `/dev/ploop38715p1` **9.8 ГБ всего, 3.2 ГБ занято (35%), 6.1 ГБ свободно**.
- `uptime`: up 4 days, **load average 0.00 / 0.01 / 0.05** (фактически idle).

## RAM (`free -m`)
```
        total   used   free   shared  buff/cache   available
Mem:     1536    262    266       31        1038         1273
Swap:       0      0      0
```
- **used 262 МБ**, available **1273 МБ**, buff/cache 1038 МБ (в основном кэш ФС, вытесняем).

## Топ процессов по памяти (RSS)
| Процесс | RSS | %MEM |
|---|---|---|
| `next-server (v15.4.11)` standalone-рантайм | **229 МБ** | 14.5% |
| postgres: checkpointer | 17.6 МБ | 1.1% |
| systemd-journald | 15.8 МБ | 1.0% |
| postgres (main, `-D .../17/main`) | 15.1 МБ | 0.9% |
| postgres: sabantuy idle conn | 10.3 МБ | 0.6% |
| + walwriter/logind/sshd/init по 5–8 МБ | | |

**Суммарно Postgres-кластер ≈ 50–60 МБ RSS** (несколько фоновых процессов + 1 idle-коннект; реальная общая память меньше из-за shared_buffers).

## БД (размер, PII)
```
sabantuy | 11 MB
postgres |  7.5 MB
```
- **`sabantuy` = 11 МБ** при минимальном контенте (3 страницы, 2 альбома галереи 13 фото, культ-разделы, demo Events, localization-таблицы `*_locales`).
- **PII почти нулевая сейчас:** `registrations=0`, `raffle_entries=0`, `subscribers=1`. В сезон вырастет (заявки на состязания/розыгрыш/подписка) — но это десятки–сотни строк ФИО+телефон, не гигабайты. Медиа (фото) лежат в `shared/media` на ФС (3.2 ГБ занято диска включает их + node_modules релизов), НЕ в БД.

## Вывод для sizing
- **Нижняя граница web-рантайма в покое: standalone next-server ≈ 230 МБ RSS.** Это пол; под нагрузкой (ISR-ревалидация, image-resize sharp на загрузке медиа) пик выше — поймаю в сезон.
- Postgres в покое держится в ~50–60 МБ — на этом боксе он не узкое место; узкое — RAM next-build (почему собираем в CI, G20) и пик рантайма.
- На 1.5 ГБ при текущей базе **запас комфортный** (available 1.27 ГБ), но `next build` сюда не влезает (OOM) — поэтому build-in-CI остаётся обязательным паттерном кластера.
- **Сезонный пик не пойман** — это главный недостающий замер; пришлю отдельно, если поймаю фестивальный трафик (важен пик RSS next-server и число конкуррентных PG-коннектов).

Готов войти в Бокс 1. Миграцию координируем, когда соберёте метрики со всех боксов.
