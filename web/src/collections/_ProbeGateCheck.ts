// ВРЕМЕННО: заведомое нарушение для приёмки дрейф-пробы G231 (AGENTS §PR-only flow:
// гейт заведён, когда показан его красный прогон). Не подключён в payload.config.ts,
// на приложение не влияет. Удаляется следующим коммитом этой же ветки.
export const ProbeGateCheck = {
  slug: 'probe-gate-check',
  fields: [],
}
